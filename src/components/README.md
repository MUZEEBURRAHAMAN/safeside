# src/components/

Real UI components live here once the app is scaffolded (Phase 0). Empty for now — you're pre-build.

When you start the Xcode/SwiftUI app, SwiftUI views should consume design tokens from an Asset Catalog + a Swift `Theme`/`DesignTokens` enum (per `docs/DESIGN_SYSTEM.md`), never raw hex. (Stack: native iOS Swift — see `docs/NATIVE_IOS_STACK.md`.) Document any new component in the design system before shipping it.
