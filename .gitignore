.DS_Store
node_modules/
.env
.env.*
*.xcuserstate
xcuserdata/
DerivedData/
.build/
ios/FoodScanner.xcodeproj/
__pycache__/
.supabase/
supabase/.temp/

# iOS — real Supabase config (secrets); ios/Config-example.xcconfig stays tracked.
ios/Config.xcconfig
# iOS — Info.plist generated by `xcodegen generate` (contains no secrets — the
# literal "$(SUPABASE_URL)" macro is substituted by Xcode at build time — but
# it's a build artifact, not authored source).
ios/Generated/

# Claude Code session/memory scratch (not app source)
.claude/projects/

# Xcode build output from -derivedDataPath ./build (worktree agents)
ios/build/
# Agent worktrees (embedded repos)
.claude/worktrees/
