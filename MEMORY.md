# MEMORY.md — Long-Term Project Memory

> A running log of decisions, why we made them, and open questions. Append-only; newest at top. This is what keeps context across sessions.

## Decisions log

### 2026-07-11 · Home redesign — how-it-works carousel + quick-action grid (Variant C)
- **What shipped:** Home's TOP restructured to the founder-picked **Variant C + swipeable how-it-works carousel**. New section stack (top→bottom): **(1) wordmark + greeting** ("SafeSide" in Space Grotesk `greenDeep` + "Know what's really in your food.") → **(2) how-it-works carousel** → **(3) quick-action grid** (Scan · Search · Categories · Favorites) → **(4) Recent scans** (unchanged grid + states) → **(5) Trending healthy** (unchanged). The old full-width `ScanCTACard` + `SearchFieldRow` were **removed** (their routes now live in the quick-action grid — no feature lost); `DailyInsightTile`, pantry filter chips/sort/context-remove, offline banner, skeletons, all empty/loading/error states **kept**. Added **pull-to-refresh** (`.refreshable` → `loadRecent`+`loadTrending`, mirrors the `.task`) since it was expected but absent.
- **Carousel build:** paged `TabView(selection:)` + `.tabViewStyle(.page(indexDisplayMode: .never))` with a **custom dot row** so dots use brand tokens (active = `green`, inactive = `greenSoft`; `accessibilityHidden`). 3 green `greenDeep` cards (AA-safe white text) — icon disc + Space-Grotesk headline + one calm line — each a combined VoiceOver element. **Reduce-Motion:** a slow 7s auto-advance timer that **no-ops when `accessibilityReduceMotion`** (manual swipe always works). Card frame is `@ScaledMetric` so it grows, not clips, at large Dynamic Type. **Scaffolded-but-commented** 4th "featured product" slide (`// TODO(home-later): featured product slide`) — NOT rendered, we have no curated pick and won't fake one.
- **Quick-action routes (honest, never a dead-end):** Scan → scanner `fullScreenCover`; Search → pushes `SearchView` in Home's own stack (push-safe); Categories → **switches to the Categories tab** via a new `onSelectTab: ((RootTab)->Void)?` closure injected from `RootTabView` (CategoriesView owns its own `NavigationStack`, so we switch tabs rather than nest a second stack); **Compare → Favorites** — chosen deliberately: there is NO two-product Compare picker in the app (CompareView only opens product-vs-swap), so per the brief the 4th tile is **Favorites**, which flips the pantry filter to `.favorites` and scrolls to it via `ScrollViewReader` (the honest pool a user would compare *from*). Tiles = `greenSoft` with `greenDeep` icon (matches Categories rows + Scan chrome); reflow 4-across → 2×2 at accessibility Dynamic Type.
- **Audit fixes folded in (docs/UI_UX_AUDIT_2026-07-11.md → Home):** (1) **top win** — added the v3 "floating card" **soft shadow** to `surfaceCard` (`.shadow(black 6%, radius 20, y 6)`) — lifts every white card app-wide (search/pantry/product/state cards) off the mint canvas, the intended cross-screen v3 upgrade; (2) the **"clear, sourced score" duplication** (hero subhead vs Scan CTA subtitle) is resolved by the restructure — the hero subhead is now the neutral greeting and the Scan CTA card is gone; (3) cheap numeric polish — `.monospacedDigit()` on the `GradeDot` score + `DailyInsightTile` count.
- **Wordmark colour honesty:** used `greenDeep` (not the lighter `green`) for the wordmark so the large bold display clears WCAG AA large-text 3:1 on canvas (plain `green` ≈ 1.8:1 fails).
- **Files:** `HomeView.swift` (top restructure + `HowItWorksCarousel`/`HowItWorksCardView`/`QuickActionTileLabel`; removed `ScanCTACard`/`SearchFieldRow`), `FoodScannerApp.swift` (new `RootTab` enum + `TabView(selection:)` + tags + `HomeView(onSelectTab:)`), `DesignKit.swift` (`surfaceCard` shadow), `docs/COPY_DECK.md` (new §Home strings). iOS **BUILD SUCCEEDED**, **61 tests pass**, no new warnings. Sim-verified 17 Pro (`SHOW_SCREEN=home`): carousel page 1 ("Scan any barcode", dot 1 green) + page 3 ("Swap for a better option", dot 3 green) via auto-advance; 4 greenSoft tiles; floating trending cards; calm empty recent state. Screenshots: `reference/screenshots/home-redesign/home-page1.png`, `home-page2.png`.
- **Honest notes / thin spots:** (a) the featured-product slide is intentionally absent (scaffolded-commented). (b) Compare is Favorites, not a real 2-product picker (none exists yet). (c) The 4th tile is labeled **"Favorites"** not "Compare" (brief-sanctioned deviation). (d) In the DEBUG `SHOW_SCREEN=home` harness (`onSelectTab == nil`) the Categories tile is inert — real app injects the closure. (e) Adding the `surfaceCard` shadow touches all 9 card-using screens; that's the v3-intended direction, but worth a visual glance on dense screens later.

### 2026-07-11 · Categories feature — bottom-nav tab, best-per-category by OUR score (Oasis-style, honest sourcing)
- **What shipped:** a **Categories** bottom-nav tab (bar is now **Home · Categories · Scan · Me** — Plan removed from the bar) → `CategoriesView` (curated category cards, Oasis `categories-01`) → `CategoryDetailView` ("Top rated · {category}", products ranked **best-first by our own sourced score**, Oasis `list-01`). SafeSide brand (green tiles, Space Grotesk, v3 floating card). iOS **BUILD SUCCEEDED**, **61 unit tests pass** (+14: 4 `FoodCategoryTests`, 10 `CategoryRankingTests`; was 47), no new warnings. **No new backend** — reads `products` ⋈ `product_current_scores` directly via PostgREST, same anon-readable pattern as `PantryService.loadTrending`.
- **HONESTY (non-negotiable, upheld):** "best in category" = **ranked by OUR transparent sourced score only**. **NO "Lab tested" / "Proven by N agencies" badge anywhere** (Oasis's `list-01` shows "Lab tested" per row — deliberately NOT replicated; we hold no such data, it would be a fabricated claim — principle #1 + teardown AVOID). Oasis's **"Unlock top rated" paywall** also NOT replicated (no paywall until Phase D). Confirmed by screenshot: real colas cohort ranked Coke-Zero 41 / Zero 41 / Coke 28 / Coke-Original 28, every row a real sourced score, zero lab/agency wording.
- **`verifiedBy` SCAFFOLD (off, tagged):** `Agency` type + `CategoryProductRow.verifiedBy: [Agency]` (ALWAYS `[]`) + `VerifiedByBadge` view that renders **`EmptyView` while empty** (always, for now). Flips on with no rewrite IF a real multi-source lab dataset is ever plugged in. `// TODO(categories-later): populate verifiedBy from a real dataset. NEVER hardcode agency names.` A unit test asserts `verifiedBy.isEmpty`.
- **Curated categories → real OFF tags** (static `FoodCategory.all`, not dynamic taxonomy — OFF tags are messy/overlapping): Sodas & colas `[en:sodas,en:colas,en:carbonated-drinks]` (densest real cohort), Beverages `[en:beverages,en:beverages-and-beverages-preparations]`, Snacks `[en:snacks]`, Sweet snacks `[en:sweet-snacks,en:confectioneries]`, Chocolate & spreads `[en:chocolate-spreads,en:sweet-spreads,en:confectionary-based-spreads]`, Biscuits & cookies `[en:biscuits,en:biscuits-and-cakes,en:biscuits-and-crackers]`, Breakfast `[en:breakfasts]`. SF-Symbol tiles (Oasis uses product photos — deferred). Lists are **thin** (2–8/category) until more products are scored — honest empty state ("No scored products here yet — scan one…", Scan action) covers it.
- **Ranking = pure `CategoryService.rank` (TDD):** join products→current score, **drop `confidence == "limited"` + non-current-`SCORE_VERSION`** (high-confidence only), dedup by product id, **sort score-desc** (name tiebreak = deterministic), cap 50. `nonisolated static` so tests hit it off-MainActor. `Scoring.currentVersion = "1.1.0"` (`// TODO(categories-later): source from a /meta read so a backend bump can't silently empty lists`). Network read guards on **client presence only** (products + `product_current_scores` are anon-readable) and keys `.task` on `session.userID` — so the list loads during guest bootstrap instead of erroring before anonymous sign-in finishes (first attempt errored; fixed).
- **Commented-out-for-later (scaffolded + `// TODO(categories-later)`, NOT deleted):** sub-category filter chips, paywall "gate top N", category-card image thumbnails, per-category count badge, dedicated /categories edge fn (client PostgREST is enough), dynamic category discovery, `verifiedBy` badge (above), **Plan tab** (`PlanView` stays in code + `SHOW_SCREEN=plan` harness; only its `.tabItem` retired), hide-empty-categories filter.
- **Files:** new `FoodCategory.swift`, `CategoryService.swift`, `CategoriesView.swift`, `CategoryDetailView.swift`, `FoodScannerTests/{FoodCategoryTests,CategoryRankingTests}.swift`; edited `FoodScannerApp.swift` (tab swap + `SHOW_SCREEN=categories|categorydetail` harness). Reused `ProductThumbnail`, compact `ScoreBadge(diameter:52,lineWidth:5)`, the "Scored" chip pattern, `ProductLoaderView(barcode:)` for taps. Plan: `docs/superpowers/plans/2026-07-11-categories-feature.md`.

### 2026-07-11 · Oasis-style UI redesign of Product / Scan / Search (Home kept ours)
- **Founder directive:** make Product-detail, Scan, and Search look **exactly like Oasis** (screenshots in `reference/oasis/`); Home stays different (ours). Confirmed treatment: **Oasis layout/structure/components + SafeSide brand (green, Space Grotesk, our tokens) + our food data** — NOT a pixel clone (avoids trade-dress/IP; keeps identity). Data mapped to food (Benefits/Additives/Processing/allergens), never Oasis's water fields (PFAS/mg-L/pH).
- **Product page** (`ProductView`/`ResultComponents`): restructured to Oasis section order — nav(wordmark+heart+share+overflow) → centered image → name+brand-link **beside** score ring (now shows **verdict WORD** e.g. "Moderately processed", fixing the audit's #1 finding) → sourced chip + confidence chip → **quick-fact dot rows** (Beneficial/Additives/Processing-NOVA/Allergens/Brand, all real data, worst-tier dots) → **"What's inside N" bordered ingredient cards** (green border safe · amber "Moderate concern" · clay "Higher concern" red-border card; category meta + KB desc + Read more) → "Why this score" meters (transparency edge kept) → "Other info" stat tiles → Sources (named+dated) → How-scoring-works + Report rows → **"Saved by N" OMITTED (no real data — refused to fabricate social proof)**. Deviations vs Oasis (all honest-data driven): concern **words** not fake "33x goal" multipliers (no ADI numbers); **clay not alarm red** (ED-safe); no category pill (no field). New: `SourcedChip`, `ScoreVerdictLabel`, `QuickFactsSection`, `OtherInfoSection`, `DisplayType.h3`.
- **Scan** (`ScannerView`/`ScanOverlay`): Oasis chrome — top-center SafeSide wordmark, **dark segmented Barcode·Photo pill** (2 segments — dropped "Shelf", we have no shelf-scan; a stub would be a dead-end), **white rounded-bracket reticle** (locks green on hit), centered "Point your camera at a barcode", spinner moved up beside reticle. Added the audit-critical **benefit-first camera-permission priming** (`.needsPriming` dark hero + "Turn on camera"/"Not now"; OS prompt only on affirmative tap — no more raw prompt over a blank canvas). Live chrome only visible on device (sim = "Camera unavailable").
- **Search** (`SearchView`): Oasis signature — **search bar pinned at the BOTTOM** via `.safeAreaInset(edge:.bottom)` (rides above keyboard) + X circle; large Space-Grotesk section header ("Recent"/"Results"); divider-free rows (thumbnail + name + brand meta + green "Scored" chip, chip ONLY when we have a current-version cached score, else "Not scored yet"); calm centered empty/error states.
- **Verified:** merged 2 worktree branches (scan+search) + product (main) → BUILD SUCCEEDED, 47 iOS tests green, screenshots match the Oasis refs, installed on device. Commits `861ea9d`(scan) `ef565ea`(search) `62d1e8f`(product) + merges → pushed `efaf10e`. Home untouched (gets the audit polish separately). Full per-screen issue backlog in [[ui-ux-audit]] (`docs/UI_UX_AUDIT_2026-07-11.md`, 73 findings).

### 2026-07-11 · Chunk 7 (Analytics + feedback + legal surfaces) — built + tests green (LAST pre-Phase-D chunk; Planner deferred)
- **Scope:** instrument the core loop with privacy-safe analytics, add the post-3rd-scan sentiment gate, and ship real Privacy/Terms/attribution screens. iOS **BUILD SUCCEEDED**, **47 unit tests pass** (11 suites; +10 new: 6 `AnalyticsLogger` + 4 `FeedbackGate`; was 37) with **no new warnings** (only the 2 pre-existing `ScannerView` iOS-17 deprecations). Backend is **one pure-SQL migration only** — `deno task test` reconfirmed **212 passed** (untouched; migration is a gate-only apply, no new Deno tests since there's no edge function).
- **No analytics edge function (reconciled):** the `events` table already had an owner-only INSERT RLS policy (`with check (auth.uid() = user_id)`), so the client inserts events **directly via PostgREST**, exactly like `ProfileService`/`PantryService`. The migration only (a) hardens `events` (`events_name_ts_idx` funnel index + `events_name_len` ≤64 CHECK) and (b) adds owner-only append-only `app_feedback(user_id, sentiment, message, ts)` so the sentiment gate's **free text lives OUTSIDE `events`** (PII never touches `events.props`). Same RLS shape as `events` (owner-only insert+select, no update/delete). Migration `20260710120000_events_hardening_feedback.sql`.
- **Canonical event names = ANALYTICS_METRICS §4 (authoritative), NOT the plan shorthand.** Wired: `scan_started`/`scan_succeeded`/`scan_failed` (source, latency_ms, found/reason — `ScannerView`), `score_viewed` (product_id, band, score, confidence — `ProductView.task`, once/appearance), `why_score_expanded` (product_id — new optional `CollapsibleSection.onExpandChanged` → `WhyScoreSection.onExpand`, default-nil so no other caller changed), `chat_opened` (product_id — `askAboutProductButton`), `swap_shown` (from_score — better-option sheet `.onAppear`; `swap_accepted` left as a `TODO(chunk-3)` since no real swap-save action exists yet), `data_reported` (product_id, reason — `ReportIssueSheet` submit-**success**, Chunk 1 is live), `feedback_sentiment` (sentiment) + `app_review_requested` (new events appended to §4). `ScanSource` enum pre-includes `.manual`/`.search` (Chunk 2) so those log without a later edit.
- **PII guard is a type + a test.** `AnalyticsValue` has only `.string/.int/.double/.bool` — no `.text`/dict case — so free text is **structurally unencodable** into props. `EventProps = [String: AnalyticsValue]`. Test `propsCarryNoFreeText` encodes a `data_reported` event and asserts props = {product_id, reason} with no message/text key. Report free-text `detail` and the gate's message are never logged; the message goes only to `app_feedback`.
- **Batched, best-effort logger.** `AnalyticsLogger` (`@Observable @MainActor`): buffers, flushes at threshold (10) and on `scenePhase→.background`, re-buffers on failure bounded at `maxBuffer` (200, oldest dropped), **never throws/blocks/delays UI**. Injectable `EventSink`; real `SupabaseEventSink` guards reachable+client+non-empty userID (returns quietly offline/unconfigured) and re-stamps `user_id` from the live session at send time (events buffered pre-sign-in carry "" → would fail RLS otherwise). `@unchecked Sendable` justified (session touched only on MainActor).
- **Sentiment gate (ED-safe, teardown #18; AVOID #8/#9).** `FeedbackGate` (pure count/flag over injectable `UserDefaults`): fires **exactly once on the 3rd successful scan** (equality, so no nag on 4th+), **never during onboarding** (scans don't count until `hasOnboarded`, AND it's presented from `RootTabView`'s `.sheet` — structurally unreachable under the onboarding `fullScreenCover`), latches `markPrompted()` on any dismiss so it never re-appears. `FeedbackGateSheet` copy VERBATIM COPY_DECK §Feedback gate: "How's SafeSide so far?" → 4 equal-weight chips (Not great/Okay/Good/Great, reflow vertical at AX sizes) → unhappy routes free text to `app_feedback` + logs `feedback_sentiment`; happy → "Mind rating us on the App Store?" [Rate SafeSide]→`requestReview()`+`app_review_requested` / equal-weight [Not now]. Tests: prompts-exactly-on-3rd, never-during-onboarding, never-reprompts, count-persists.
- **Legal screens (⚠️ LEGAL REVIEW REQUIRED).** New `LegalViews.swift`: `PrivacyPolicyView` (discloses analytics per §8: no third-party ad/tracking SDKs, data minimization, guest-first, events/app_feedback usage; keeps health disclaimer), `TermsView`, `AttributionView` (OFF/ODbL intro + link + share-alike note + USDA public-domain line — VERBATIM COPY_DECK §Legal & attribution). All on DESIGN_SYSTEM_V3 tokens (SectionCard/DisplayType/greenDeep), not a boilerplate wall (AVOID #12). Replaced the placeholder `PrivacySheet`/`DataSourcesSheet` in `MeView`; About rows now "Privacy policy · Terms of use · Data sources & attribution" (deck order) + kept "How scoring works". **The Privacy/Terms BODIES are DRAFTS in the deck's voice — NOT reviewed legal copy; founder/legal sign-off is a ship gate** (mirrored in COPY_DECK §Legal bodies with the ⚠️ flag; contact emails privacy@/hello@safeside.app are placeholders).
- **Screenshot-verified (iPhone 17 Pro, default + AX5):** PrivacyPolicyView, TermsView, AttributionView, updated Me About rows, and FeedbackGateSheet — all clean, on-brand, chips reflow vertically at AX5 with no clipping. New DEBUG harness entries `SHOW_SCREEN=privacy|terms|attribution|feedback` (feedback's `.sheet` sits INSIDE the harness content so its environment is in scope — the outside-scope version crashed "No SessionService found"; production RootTabView is fine because the parent injects the environment).
- **Files — new:** `ios/FoodScanner/{AnalyticsLogger,FeedbackGate,LegalViews}.swift`, `ios/FoodScannerTests/{AnalyticsLoggerTests,FeedbackGateTests}.swift`, `supabase/migrations/20260710120000_events_hardening_feedback.sql`. **Edited:** `FoodScannerApp.swift` (inject analytics+gate, scenePhase flush, gate sheet, harness entries), `ScannerView.swift` (scan events + scan-count), `ProductView.swift` (score_viewed/chat_opened/swap_shown/why_score_expanded + data_reported wiring), `ResultComponents.swift` (`CollapsibleSection.onExpandChanged`, `WhyScoreSection.onExpand`, `ReportIssueSheet` data_reported), `MeView.swift` (legal rows + routing, deleted placeholders), `docs/{ANALYTICS_METRICS,COPY_DECK}.md`. Plan: `docs/superpowers/plans/2026-07-10-chunk-7-analytics-feedback-legal.md`.
- **NOT deployed / DEPLOY GATES:** (1) **KEY ROTATION FIRST** — rotate the pasted Cerebras/Groq/USDA keys + the Supabase personal access token before analytics goes live (STATE.md standing rule; still outstanding). (2) Apply migration `20260710120000` (+ the still-unshipped Chunk 1–4 migrations/functions) — then RLS psql proof for `events` + `app_feedback` (owner-only insert/select, no cross-user, no update/delete) is gate-only (not runnable here). (3) **Founder/legal review of Privacy + Terms bodies** before merge/ship. (4) Device run: confirm event rows land in `events` with correct names + `user_id` = session id + no PII in props; gate appears only after 3rd scan, never in onboarding; a "Not great" submit writes to `app_feedback` not `events`. Note timing: the gate's `.sheet` presents from the tab shell the moment `shouldPrompt` flips (during the 3rd result push) — verify on device it doesn't collide with the navigation push.

### 2026-07-10 · Chunk 6 (Robustness / offline / a11y sweep) — built + tests green (iOS-only, no backend)
- **Scope:** the pre-Phase-D hardening sweep. No backend/schema/RLS/edge change — `deno task test` reconfirmed **212 passed** (untouched). iOS **BUILD SUCCEEDED**, **37 unit tests pass** (9 suites; +3 new: `APIError mapping`, `NetworkMonitor`, `Shatter snapshot orientation`; was 21). Only the 2 pre-existing `ScannerView` iOS-17 deprecation warnings — **no new warnings**. Deliverable = the `docs/TEST_PLAN.md` **§6** per-screen pass/fail checklist.
- **First-class offline (TDD):** `APIError` gained `.offline` (distinct from `.transport`) via pure `mapURLError(_:)` — `notConnectedToInternet`/`timedOut`/`networkConnectionLost`/`cannotConnectToHost`/… → `.offline`, else `.transport`. `request(_:)` + `search(_:)` catches now call it. New `NetworkMonitor` (`@MainActor @Observable` `NWPathMonitor` wrapper, optimistic-online, injectable `init(testMode:)`/`apply(status:)` seam) registered in `FoodScannerApp` + injected via `.environment`; **informs the UI only, never gates requests** (offline-first). New reusable `OfflineBanner` (neutral gray, scaled `wifi.slash`, optional Retry — never alarm-red per AVOID #5; cached content stays visible per AVOID #14). Home shows it above the pantry when `!isOnline`; cached grid still wins over the error card. Contrast of the banner (textSecondary `#5A6B60` on surfaceAlt `#EEF6EA`) computes **≈5.1:1 → passes AA**.
- **Full 429 client path (Chunk 6 owns it):** `APIError.rateLimited(retryAfterSeconds: Int?)` now carries the parsed window. On 429 `request(_:)` decodes the **body** (`retryAfterSeconds`, Chunk-4 contract) **and** the `Retry-After` **header** via pure `parseRetryAfter(header:bodySeconds:)` (body wins; non-positive/garbage → nil). `ChatViewModel` starts a per-second `rateLimitSecondsRemaining` countdown; while > 0 the **input field, Send, and Retry are all disabled** with the calm COPY_DECK line, auto-re-enabling when it clears (default 60 s when the server is silent). Unit-tested end to end on the pure helpers.
- **"Something went wrong" eliminated (grep gate = 0):** added COPY_DECK §Errors **"Server hiccup" = "That didn't load right. Give it a moment and try again."** (the one new string, drafted in the deck's voice) → now `.badResponse`/`.decoding` copy. Swapped every banned site: `ChatView` fallbacks → `APIError.badResponse.errorDescription!` (+ `.offline` → chat-specific "Chat needs a connection. Your product details are still here."), `HomeView` preview StateCard, `IngredientsLoadErrorView` heading (+ new `isOffline` variant "You're offline"), `ScannerView` ×4 error fallbacks + the `.error` banner title, all via a new `ScanViewModel.scanErrorMessage(for:)` that maps `.offline` → the scan line "You're offline. Scanning needs a connection — your pantry still works."
- **Offline never dead-ends (per-surface):** Home banner + Retry; Result opens cached score/details offline with the ingredient offline variant; Scan shows the scan-offline line and keeps manual-entry reachable; Chat shows the offline bubble; Search/Swaps already carry "check your connection" error+retry copy (offline maps there — no change needed); Me inline retry rows already honest. **Honest caveat kept:** offline cold-launch on a fresh install shows the **calm empty pantry**, not an error (verified: `PantryService.loadRecent` early-returns without setting `loadError`).
- **Shatter-orientation edge (TDD):** pure `CameraViewController.shatterImageOrientation(connectionApplied:)` (portrait-applied→`.up`, unrotated landscape-native→`.right`); `setUpSession` records `videoConnectionRotated`; `captureSnapshotForShatter` stamps that orientation on the `UIImage` so the shard grid is upright even when `isVideoOrientationSupported` was false. Final visual proof is **GATE-ONLY** (scan on device).
- **Portrait lock explicit:** `project.yml` now sets `UISupportedInterfaceOrientations` (iPhone = Portrait; iPad = both portrait variants) — confirmed present in the generated `FoodScanner-Info.plist`. Rotation crash-free walk is GATE-ONLY.
- **a11y sweep:** VoiceOver labels present in code across all flows (device swipe = GATE-ONLY); Result **sim-verified at AX5** (no clipping, name truncates by design); no new AA contrast failure surfaced on locked DESIGN_SYSTEM_V3 tokens → **0 FAILs filed** (policy: file, don't recolor — none to file). Reduce Motion re-confirmed after the snapshot change.
- **Files — new:** `NetworkMonitor.swift`, `FoodScannerTests/{APIErrorMappingTests,NetworkMonitorTests,ShatterOrientationTests}.swift`. **Edited:** `APIClient.swift`, `ChatView.swift`, `ScannerView.swift`, `ProductView.swift`, `HomeView.swift`, `ResultComponents.swift`, `FoodScannerApp.swift`, `ios/project.yml`, `docs/COPY_DECK.md`, `docs/TEST_PLAN.md` (§6 addendum). Plan: `docs/superpowers/plans/2026-07-10-chunk-6-robustness-offline-a11y.md`. **Remaining GATE-ONLY:** on-device VoiceOver swipe, real airplane-mode script, camera shatter-orientation visual, rotation walk, full AX5 device walk.

### 2026-07-10 · Chunk 5 (Compare v1) — built + tests green (iOS-only, no backend)
- **What shipped:** SCREEN_SPECS §10 Compare — two already-scored products side by side. New `CompareView.swift`: two-column header (`FloatingProductImage` + name/brand + reused compact `ScoreBadge`), then aligned metric rows (**Overall + Nutrition + Additives + Processing**) where each row stacks both products' meters on ONE fixed 0–100 scale so bar lengths are directly comparable ("shared scale!"). Per-row **winner tint is a subtle band-colour wash on the winning side's meter only** (opacity ~0.12), using that side's OWN band colour — so the higher of two mid scores washes amber, never green-under-low (AVOID #5), and never alarm-red (AVOID #1). CTA "Pick this one" (primary green for the overall winner's column, quiet outline for the other — never a "loser" penalty) → `pantryService.save(product:)` → inline "Saved to pantry." → auto-`dismiss()` (never a dead-end). Verified on-sim: 17e/17pro/promax, default + AX5.
- **Compare logic is pure + tested (TDD, LLM-never-does-math):** new `ComparePair` value type in `Models.swift` — reads backend integers (`ScoreResult.score`, `ScoreFactor.subScore`), decides which side reads higher per metric; never computes a score. `winner(_:_:)`: higher wins, equal = tie, **any unknown side = tie** (an unscored side never wins — honest-state rule). `rows` always emits 4 rows in fixed order regardless of backend factor order; a missing factor on a side → that side `nil` + tie. New `CompareLogicTests.swift` (8 cases: winner basics/unknowns, fixed 4-row order from scrambled input, per-metric winners, missing-factor tie, thin-side ties, overallWinner, `ScoreBand.tint`). iOS test suite now **21** (was 13).
- **Meter reuse decision (important for Chunk 6 / whoever touches meters):** Chunk 1 already shipped `MeterRowView`/`MetersSection` — but those are **`MeterRow`(nutrient)-driven** (value/unit/tier word, tier-based tint, tap-for-sources). Compare compares **factor sub-scores (Int 0–100 + band)** — a genuinely different data shape, so I added a sibling primitive **`SharedScaleMeter`** (value:Int? + band, dashed track when nil, subtle `emphasized` wash) rather than abusing `MeterRowView` with synthetic `MeterRow`s. They are NOT duplicated logic — different input/tint semantics; `MeterRowView` still owns the nutrient meters unchanged. **The real de-dup:** extracted the band→colour `switch` (previously copied in `ScoreBadge`, `TriMetricTile`, `ScoreFactorRow`) into a single **`ScoreBand.tint`** (in `Theme.swift`) and adopted it in all three + `SharedScaleMeter`. One colour source now. (If a future chunk wants one bar primitive, `MeterRowView`'s `ProgressView` could sit on `SharedScaleMeter` — not done now to keep Chunk 1's shipped look pixel-identical.)
- **Compact reflow:** iPhone has no split-view width class, so `dynamicTypeSize.isAccessibilitySize` is the "compact" proxy (matches `ProductView`'s existing reflow guard). Compact = a **sticky segmented `Picker`** (A/B, product names truncated at 18 chars + ellipsis, COPY_DECK) above the scroll; selecting a side shows that one product's single-column card with its meters still on the shared 0–100 scale (+ a neutral "Higher" chip on rows it wins). Verified AX5: sticky toggle, truncated names, ring not clipped.
- **Entry points:** Result-overflow is primary + shipped — added an `ellipsis.circle` `Menu` ToolbarItem beside the heart (both 44 pt) → "Compare" opens a partner-picker sheet (`ComparePartnerPicker`, private) listing `pantryService.entries` (`entry.asProduct()`) minus the current product → tap pushes `CompareView` via `navigationDestination(item:)`. `Product` isn't `Hashable`, so the pushed value is a tiny id-keyed `ComparePartnerRoute` box (mirrors SearchView's `BarcodeRouteBox`). **Search multi-select entry is DEFERRED** (clean `TODO(chunk-5 follow-up)` in `SearchView.swift` + `CompareView.swift`) to keep this chunk minimal — the plan gates it and it adds selection-UX risk; Result-overflow satisfies the exit criteria alone.
- **Thin-partner honesty (for Chunk 6):** a pantry-picked partner is a **thin** `Product` (`asProduct()` has `factors: []`), so on real devices its three factor rows show the dashed nil meter + tie while **Overall still compares** (score is cached). This is the honest v1 (never fabricate a sub-score); the screenshot matrix uses two FULL `.sampleScored*` fixtures via the DEBUG `SHOW_SCREEN=compare` harness to show a real two-column contrast. A future v1.1 could re-fetch the partner's full score before comparing.
- **Copy:** all strings verbatim from COPY_DECK §Compare; added ONE new line — **"Higher"** (compact per-row winner chip; neutral, marks the shown side's own strength, never labels the other side). Banned-word grep on `CompareView.swift` clean (only internal `winner`/`overallWinner` identifiers); `font(.system(size:` grep clean.
- **Verified:** iOS **BUILD SUCCEEDED** + **TEST SUCCEEDED** (iPhone 17 Pro sim; 21 tests; only the 2 pre-existing `ScannerView` iOS-17 deprecation warnings, none new). Backend untouched → `deno task test` reconfirmed **212 passed, 0 failed**. Files: **new** `CompareView.swift`, `FoodScannerTests/CompareLogicTests.swift`; **edited** `Models.swift` (+`ComparePair`), `Theme.swift` (+`ScoreBand.tint`), `ResultComponents.swift` (+`SharedScaleMeter`, adopt tint ×2), `ScoreBadge.swift` (adopt tint), `ProductView.swift` (overflow menu + partner picker + nav), `PreviewSupport.swift` (+`sampleScoredHigh`), `SearchView.swift` (deferred-entry TODO), `FoodScannerApp.swift` (`SHOW_SCREEN=compare` harness), `docs/COPY_DECK.md`.

### 2026-07-10 · Chunk 4 (Data & correctness wave) — built + tests green (deploy pending)
- **`product_current_scores` view (fixes highest-ever bug):** migration `20260710020000_current_scores_view.sql` — `select distinct on (product_id) … order by product_id, computed_at desc` → exactly one row per product = its LATEST score (real column is **`computed_at`**, not MASTER_PLAN's fictional `scored_at`). `with (security_invoker = true)` so it inherits `score_results`' global-read RLS — **no new write surface**; `grant select … to authenticated, anon`. Now the single source of truth for "the score a user sees today"; resolves the two `// TODO(chunk-4)` sites: `search/index.ts` `getScoresForBarcodes` + `swaps/index.ts` `currentScoresFor` both read the view (transparency rule intact — only OUR band at `SCORE_VERSION`, band ≠ unknown, never OFF's Nutri-Score). iOS `PantryService.loadTrending`/`loadRecent` swapped `.from("score_results")` → `.from("product_current_scores")`; trending keeps its `band == high` + `score` desc order (now filters the *current* score, the bug fixed); the per-product de-dup loops are harmless no-ops kept for a future fallback. Stale ASSUMPTION doc-comment rewritten.
- **OCR parenthetical-orphan fix (TDD):** `product-ocr/handler.ts` — new `ADDITIVE_CLASS_WORDS` set + `isAdditiveClassOrphan(part)`: a comma-segment that is just a class word (colour/flavour/emulsifier/preservative/antioxidant/…) whose parenthetical resolves to an additive (E-number OR a `ADDITIVE_NAME_TO_TAG` name) is skipped as a display token — the additive tag is already captured. `"Colour (E150d)"` no longer emits a stray "Colour"; `"Citric Acid (E330)"` is KEPT (real ingredient). Comma-in-parenthetical `"Colour (E150c, E150d)"` handled: the orphan regex's closing paren is optional so the `"Colour (E150c"` fragment still matches+drops, and `cleanToken` now drops a bare-E-number token (`^e\s?\d{3,4}[a-z]?$`) so `"E150d)"` doesn't leak. 5 new regression tests (incl. the comma case) + the choco-spread fixture now asserts "Emulsifier" absent.
- **`/chat` rate-limit (TDD):** pure `withinRateLimit(timestamps, now, windowMs, max)` (sliding window; `retryAfterSeconds` = time until the oldest in-window hit ages out) + optional `Deps.userId/recentChatRequests/recordChatRequest` (absent → limiter skipped, backward-compatible with offline tests). On exceed → **429** with `Retry-After` header + `{ error:"rate_limited", retryAfterSeconds, reply:<COPY_DECK calm line>, disclaimer }`; records only ACCEPTED requests so a 429 never extends its own window. `RATE_LIMIT_WINDOW_MS=60_000`, `RATE_LIMIT_MAX=10`. `chat/index.ts` decodes JWT `sub` (anon uids too; unverified — platform already verified) + wires the ledger against a new **service-role-only** `chat_rate_events` table (migration `20260710020100`; RLS on, **no client policies** → deny-all). COPY_DECK line 151 already existed → verbatim. iOS: `APIClient` gained `.rateLimited` (429 → the calm line) rendered by ChatView's existing ErrorBubble (Try-again, never a dead-end). 6 new chat tests.
- **Re-score cron on `score_version` bump (TDD):** new `rescore/` edge fn — pure `handleRescore(req, deps)` + `rescoreProduct(product)` reconstructs inputs from stored `raw_off` (`source==="off"` → `mapOffPayload` → `mapAdditiveTiers` → `computeScore` + unreviewed-additive note; `source==="ocr"` → `raw_off.parsed.additivesTags`, NOVA/Nutri null → band **unknown**), inserts a fresh `score_results` row (history preserved → the view serves it). `?dryRun=1` reports without inserting; unmappable `raw_off` skipped (never aborts the batch); response `{ scanned, rescored, skipped, dryRun, products:[{id,from,to}] }`. `index.ts` service-role client; `findStaleProducts` diffs `products` vs `product_current_scores.score_version` (incl. unscored). **Secret-gated, NOT publicly triggerable:** `X-Rescore-Secret` must equal `RESCORE_SECRET` env → else 401. Schedule migration `20260710020200_rescore_cron.sql` (pg_cron + pg_net, daily 04:00 UTC, `limit:200`, secret from `current_setting('app.rescore_secret')` so the literal is never committed). 7 new tests. **LLM never does the math** — re-score is the deterministic engine.
- **Dietitian weight review (no code change):** `docs/reviews/SCORE_WEIGHTS_REVIEW_2026-07.md` (+ `.csv`) — composite weights, NOVA/Nutri maps, additive penalty tiers, the full **124-entry** additive risk table (84 low / 35 moderate / 5 higher, each with its regulatory source), and the **50-product** calibration set (inputs → expected sub-scores/score/band/notes) with an "Agree? (Y/N)" column + sign-off. Standing instruction baked in: **any weight/tier change → bump `SCORE_VERSION` in `engine.ts`, log in MEMORY.md, and the Task-5 cron auto-re-scores every cached product.** `score_version` still **1.1.0** (unchanged this chunk).
- **Verified:** `deno task test` **212 passed** (was 195; +5 OCR, +6 chat rate-limit, +7 rescore, −1 net from folding an OCR assertion into an existing test) · `deno lint` clean on touched dirs · `deno fmt` applied to touched files only (untouched files carry a pre-existing fmt-version discrepancy — not this chunk's to churn). iOS **BUILD SUCCEEDED** + **TEST SUCCEEDED** (iPhone 17 Pro sim; no new warnings).
- **DEFERRED to the backend-deploy gate (NO live Supabase this chunk):** apply migrations `20260710020000/020100/020200`; `supabase functions deploy chat rescore` (+ still-pending Chunks 1–3 deploys); `supabase secrets set RESCORE_SECRET=…` + `alter database … set app.rescore_secret=…` (both out of repo). **SQL cross-check the gate runs** (proves current not highest-ever): for a product with an old high row + a newer lower row, `select score,computed_at from product_current_scores where product_id=:pid` must equal that product's `MAX(computed_at)` row from `score_results` (the newer, LOWER score), NOT its `MAX(score)` row. Burst-test `/chat` (12× → 429 + `Retry-After` + calm line). `/rescore?dryRun=1` with the secret reports a stale product; real run inserts a `SCORE_VERSION` row the view then serves. Device: trending shows current score; chat 429 renders the calm line. **For Chunk 6:** the full 429 client path (respect `Retry-After`, disable the input N seconds) — Chunk 4 only surfaces the calm line via the error bubble. **For Chunk 7 (analytics):** consider a `chat_rate_limited` event when the 429 lands.

### 2026-07-10 · Chunk 3 (Swaps engine — "See a better option") — built + tests green (deploy pending)
- **Done (backend, pure + tested):** new `swaps/` edge fn mirroring `chat/`'s split — `handler.ts` (pure `handleSwaps(req, deps)`, `rankSwaps`, `whyBetter`, `extractSubjectId`) + `handler_test.ts` (16 cases, all fake deps, no network) + `index.ts` wiring. Endpoint `GET swaps/product/:id/swaps` → `{ category, subjectScore, filteredForAllergies, swaps[], thin }`; 401 no-auth, 400 non-uuid, 404 subject-not-in-`products`, 200 honest-empty (`swaps:[]`, `thin:true`) when subject has no category or nothing beats it. **Ranking is deterministic** (SCREEN_SPECS §5): same OFF category (array `&&` overlap) → strictly-higher current score (skip null/unknown-band) → allergen-safe (drop candidates whose `allergens_tags` intersect the caller's profile allergies; tracks `filteredForAllergies`) → stable sort inPantry↓ · delta↓ · score↓ · name↑ → cap 5, `thin` when <2. **"Why better" is 100% DB-diff, no LLM (principle #5):** additive-absence facts only for **moderate/higher**-tier reviewed additives (from `additives_risk.json`, display `eNumber` + INS class via the shared `additiveCategory` → `"No colours E150d"` verbatim from COPY_DECK) + per-100 g `lower {sugar|saturated fat|salt}` when candidate <90 % of subject; capped at 3, additives first.
- **Score attachment (reused verbatim from Chunk 2):** candidates/subject carry a score ONLY at the current `SCORE_VERSION` and band ≠ `unknown` — never OFF's Nutri-Score (teardown AVOID #5). `currentScoresFor()` reduces newest-per-product `score_results` in JS; `// TODO(chunk-4)`: swap to the `product_current_scores` view. **RLS split (mirrors `chat/index.ts`):** service-role client reads the global `products`/`score_results` cache; a **user-scoped (JWT) client** reads `pantry_items` + `profiles.allergies` under RLS — any auth problem → empty set/[] (guest still gets category-ranked, unfiltered swaps; never crashes).
- **Schema/data:** migration `20260710000000_products_categories.sql` adds `products.categories_tags text[]` + a GIN index (for the `&&` query); `_shared/off.ts` `OFF_FIELDS`/`OffProduct`/`mapOffFields` now fetch+map `categories_tags` (Chunk 2 hadn't added it); `product/handler.ts` + `product-ocr/handler.ts` (OCR → `[]`, no OFF category) pass it through on upsert. Because products only enter the DB on scan, **seed** `20260710010000_swaps_demo_seed.sql` writes a `en:chocolate-spreads` cohort (5 products + current-version scores spanning bands; SUBJECT id `aaaaaaaa-0000-4000-8000-000000000001`, score 29) so a low-score demo returns ≥1 real better option and allergen filtering is provable. Both migrations + seed apply at the **deploy gate** (no live Supabase this chunk).
- **Deviation (justified):** added `barcode` to the swap row/response/`SwapCandidate` (not in the plan's field list) so the card's **View** action routes through the shipped `ProductLoaderView(barcode:)` — the identical scored `/product/:barcode` path + skeleton + never-dead-end recovery — instead of pushing a thin `Product`. Candidates always have a barcode (OCR-only rows have empty `categories_tags`, so never candidates).
- **iOS:** `Models.swift` `SwapCandidate` + `SwapsResponse` (Codable). `APIClient.swaps(productID:)`. New `SwapsView.swift` sheet (detents medium/large): title "Better options in {category}", calm restriction note, loading skeleton cards, ranked `SwapCard` (thumb + name/brand + **mini score ring** + band-tinted delta chip `+N score` (tabular digits, never red) + `whyBetter` joined ` · ` + "In your pantry" chip + View/Save actions), honest empty/thin note reusing "Few close matches…"+"Scan another" (the retired stub's behaviour), and error retry. `ScoreBadge` got a compact initializer (`diameter:lineWidth:` defaulting 108/9; swaps pass 52/5) — same redundant number+word+arc ring, never color-only. `PantryService.saveToPantry(productID:)` — thin id-only reuse of the `upsertStatus` path (no fake `Product`). **Rewired** `ProductView`'s "See a better option" CTA from `NextActionSheet` → `SwapsView(product:onScanAnother:)`; **deleted** the `NextActionSheet` stub + its preview (only ProductView referenced it). Card reflows to vertical at accessibility Dynamic Type sizes; combined a11y label per card ("Better option: X, score 78, …") with View/Save separately focusable.
- **Copy:** added 1 string to `docs/COPY_DECK.md` §Swaps — Error: "Couldn't load better options. Check your connection and try again." [Retry]. All other swaps strings (title/loading/delta/why-better pattern/actions/pantry chip/empty/restriction) already existed in the deck; used verbatim.
- **Verified:** `deno task test` **195 passed** (was 176; +3 off `categories_tags` mapping, +16 swaps ranking/why-better/auth). iOS **BUILD SUCCEEDED** (no new warnings — the 2 ScannerView orientation deprecations pre-date this chunk) + **TEST SUCCEEDED** (13 tests / 5 suites, incl. new `SwapsDecodingTests` — loaded + honest-empty decode, band round-trip). `deno fmt` applied to the new `swaps/` files (shared files left untouched — their pre-existing fmt-version discrepancies aren't from this chunk; `deno fmt --check` isn't the gate).
- **DEFERRED to the backend-deploy gate (NO live Supabase this chunk):** `supabase db push` (migrations `20260710000000_products_categories` + seed `20260710010000_swaps_demo_seed`) + `supabase functions deploy swaps`. **Smoke test:** `GET swaps/product/aaaaaaaa-0000-4000-8000-000000000001/swaps` with a valid bearer → ≥1 swap with non-empty sourced `whyBetter` (e.g. "No colours E150d · lower saturated fat") + correct `delta`; repeat with a test profile carrying a **milk** allergy → the "Dark Cocoa Spread" candidate is filtered and `filteredForAllergies:true`; a no-category product → `swaps:[]`, `thin:true`. Also still pending from Chunks 1–2: deploy `search`/`product-report`/`product`/`ingredients` + the Chunk-1 migration. Device install + founder review + 6-shot Swaps matrix (loaded/thin/error × 17e/17 Pro/17 Pro Max × default/AX5) also pending.

### 2026-07-10 · Chunk 2 (Search + manual barcode entry) — built + tests green (deploy pending)
- **Done (backend, pure + tested):** refactored `_shared/off.ts` — extracted `mapOffFields(p, code)` (the one place OFF field-mapping lives; `mapOffPayload` now calls it, unchanged behavior) and added `mapOffSearchPayload(payload)` + `fetchOffSearch(query, fetchImpl=fetch)` (OFF v2 `/search`, `search_terms`+`fields,code`+`page_size=20`+`sort_by=unique_scans_n`, `encodeURIComponent` not URLSearchParams so spaces are `%20`, throws on non-OK → 502). Kept the search helpers **general** (no Search-screen assumptions) so **Chunk 3 (Swaps) reuses this plumbing**. New `search/` edge fn mirroring `product/`: `handler.ts` (pure `handleSearch(req, deps)` — GET/OPTIONS/405, 401 auth, 400 `empty_query`, 100-char truncate, 502 on OFF throw) + `index.ts` wiring (service-role `getScoresForBarcodes` = products barcode↔id then newest `score_results` per product). **Transparency gate baked in:** a row carries `score` ONLY when it's OUR cached score at the **current `SCORE_VERSION`** and band ≠ `unknown` — OFF's own Nutri-Score is never surfaced (teardown AVOID #5); unscored rows omit `score` → neutral "Not scored yet" client affordance. `// TODO(chunk-4)`: swap the score lookup to `product_current_scores` when it lands.
- **iOS:** `Models.swift` `SearchResult` (Codable/Identifiable, `score: SearchScore?` = nil ⇒ neutral). `APIClient.search(query:)` builds the URL with **`URLComponents`/`queryItems`** (NOT the path-appending `request(_:)`, which percent-encodes `?`/`=`). New `SearchView.swift`: `SearchQuery.classify` (regex `^\d{6,14}$`, same bounds as backend `BARCODE_RE`; unit-tested), `@Observable @MainActor SearchViewModel` (recents/searching/results/noResults/error, 300 ms debounce; barcode → route straight to the scored `/product/:barcode` path, name → `/search`), the screen (field + magnifier `@ScaledMetric`, barcode-mode toggle clearing the field, `.numberPad`), `SearchResultRow` (grade dot only when scored, else "Not scored yet" + chevron), and `ProductLoaderView(barcode:)` — fetches the identical scored path a scan uses, shows the **real `ResultSkeletonView`** while loading (now production-live), auto-saves to pantry on success, and never dead-ends (not-found → "Snap the label", error → Retry). Home gets a tappable `SearchFieldRow` (button-that-looks-like-a-field) under the hero → pushes Search. Scan `needsOCR`/`labelNotFound`/`error` banners gained a quiet "Enter barcode manually" tertiary action → presents `SearchView(startInBarcodeMode: true)` as a sheet (dark scan chrome intact underneath).
- **Copy:** added 3 drafted strings to `docs/COPY_DECK.md` §Search & manual barcode — empty-recents ("No recent scans yet. Search a product name, or enter a barcode." [Scan instead]), loader not-found (reuses the scan-side "We don't have this one yet…" line), unscored-row caption ("Not scored yet"). Used verbatim.
- **Design call:** `ResultSkeletonView`'s in-flight wiring is now **production-live via `ProductLoaderView`** (it renders the skeleton during a real fetch), NOT via `ProductView`'s internal `isResolving` flag (that stays DEBUG/preview-only). Reason: the not-found/error recovery must wrap the fetch (principle #4, never a dead-end), which `ProductView`'s always-render body can't express — so the loader owns the fetch + skeleton + recovery and hands a fully-resolved `Product` to `ProductView`. Recents rows route through `NavigationLink → ProductView` (thin-read enrichment, same as Home); search rows route through the loader.
- **Verified:** `deno task test` **176 passed** (was 157; +19 new across off + search-handler suites). iOS **BUILD SUCCEEDED** (no new warnings — the 2 ScannerView orientation deprecations pre-date this chunk) + Swift Testing **11 tests / 4 suites passed** incl. new `SearchQuery.classify` (4 tests). Home screenshot confirms the search field renders + reflows at large Dynamic Type. Grep gate clean (only tap-target `minHeight` 44/52/36 — the established no-height-token convention — and the `GradeDot` `13 * dotScale` glyph pattern).
- **DEFERRED to the backend-deploy gate (NO live Supabase this chunk):** `supabase functions deploy search` (verify_jwt default on) — until then the in-app Search screen errors against the live project. Also still pending from Chunk 1: `db push` migration `20260710000000` + deploy `product-report`/`product`/`ingredients`. Then curl-smoke `search?q=nutella` (≥1 result, scored ones carry `score`) + confirm a typed barcode opens the identical scored Result. Device install + founder review + the 6-shot screenshot matrix (recents/searching/results/no-results/error + Home-with-field + Scan banner × 17e/17 Pro/17 Pro Max × default/AX5) also pending.

### 2026-07-10 · Chunk 1 (Result screen upgrade) — built + tests green (deploy pending)
- **Done (backend, all pure + tested):** `_shared/scoring/meters.ts` — `buildNutrientHighlights(nutrients, additiveTiers)` turns the already-stored per-100 g `products.nutrients` into `watchOuts[]`/`benefits[]` bar-meter rows (label, value rounded once, unit, neutral tier word, 0…1 meterFraction, sources) + `toKnowAboutCount`/`beneficialCount`. Thresholds are **FSA front-of-pack** (sat fat 1.5/5.0, sugars 5.0/22.5, salt 0.3/1.5 g) for watch-outs and **EU "source of"/"high in" fibre + protein** for benefits — all cited in `MeterRow.sources`, no unsourced number. **Energy/calories are never emitted** (ED-safe). Wired into `product/handler.ts` `buildBody()` on `score.highlights` so it serves on **both cache-hit and fresh paths** (additive tiers recomputed from stored tags) — **no score_version bump, no re-score**. Added `fetchedAt` to the product body (for dated sources).
- **Additive category** (`_shared/additives/category.ts`): `additiveCategory(E-number)` buckets by pure **INS numeric class range** (100s Colours, 200s Preservatives, 300s Antioxidants, 400s Thickeners & emulsifiers, 500s Acidity regulators, 600s Flavour enhancers, 900s Sweeteners, else Other) → attached as `category` on each additive ingredient in `ingredients/handler.ts` (derived at response-shaping time, not cached; plain food tokens → null). **Deliberate call:** E322 (lecithin) reads "Antioxidants" by numeric range, not "Thickeners & emulsifiers" — the plan authorised choosing pure range math (no per-entry table), so lecithin's functional class loses to its 300-series number.
- **Report round-trip:** new `product_reports` table (migration `20260710000000`) + insert-only RLS mirroring `events`; new `product-report` edge function (service-role insert, JWT-derived nullable `reporter_id`, reason enum + 1000-char detail cap). iOS `APIClient.reportIssue(...)` + a real `ReportIssueSheet` (reason single-select → sending → "Thanks — we'll review it." / Retry-on-fail).
- **iOS:** `Models.swift` gained `MeterRow`/`NutrientHighlights` + optional `ScoreResult.highlights`, `Product.fetchedAt`, `Ingredient.category` (all `var … = nil` so pre-Chunk-1 cached responses + every preview/construction site still decode/compile untouched). New reusable `MeterRowView`/`MetersSection` (zero client arithmetic — the view only maps tier→colour), `IngredientCountPreRead`, `AdditiveSummaryRow`/`AdditivesSummarySection`, dated `SourceLink`/`SourcesSection` (only OFF-provenance rows get "· updated {date}"). `ResultSkeletonView` wired into `ProductView` behind a dormant `isResolving` flag (Chunk 2 flips it on for Search/deep-link). Favorite glyph already semantic from Chunk 0.
- **Copy:** added 3 sets to `docs/COPY_DECK.md` §Result upgrades — additive severity words (Lower/Moderate/Higher concern), 8 INS category pill labels, and the full meter tier-word ladder (watch-outs low/moderate/high · benefits low/some/good source). Backend tier strings match the deck verbatim.
- **Verified:** `deno task test` **157 passed** (was 121; +meters 10, +category 10, +product-report 11, +product 4, +ingredients 1). iOS **BUILD SUCCEEDED** + **TEST SUCCEEDED** (new `HighlightsDecodingTests` — decodes highlights/category/fetchedAt AND a legacy body without them). No new warnings; grep gate clean (only the documented shippingbox placeholder literal remains).
- **DEFERRED to the backend-deploy gate (NOT done — no live Supabase writes this chunk):** apply migration `20260710000000_product_reports.sql` (`supabase db push`), deploy `product-report`, and re-deploy `product` + `ingredients` (so `score.highlights` + `category` reach the live app). Then smoke: report endpoint → 201 + row lands; `product/:barcode` → body carries `highlights` + `fetchedAt`. Device install + founder review + the 6-shot screenshot matrix also still pending.

### 2026-07-10 · Chunk 0 (UI hygiene) shipped & verified — first slice of the revamp
- **Done:** `Theme.Space.s45=20` token + off-grid padding literals replaced (Home/Me/Plan); 4 banned "Something went wrong" strings → honest, specific copy (PantryService/ProfileService); `@ScaledMetric` glyph scaling across 9 sites so icons grow with Dynamic Type (ScanCTA, DailyInsightTile, GradeDot, favorite hearts, OptionCard, Me guest avatar, Plan hero icon; ProductView nav-heart moved to semantic `.body`); Me tab profile-load error retry row; Onboarding allergen subline + "change anytime in Me" reassurance; **pantry item removal** (`PantryService.remove()` optimistic + Supabase delete + reload-on-fail) with context-menu + confirm dialog, plus a recent/score sort menu.
- **Why:** cheapest, highest-confidence gaps from `docs/SCREEN_SPECS.md` (fixed glyph sizes, off-grid padding, silent errors, no pantry removal) — clear them before the bigger Result/Search/Swaps chunks so later work builds on clean tokens + honest copy.
- **Verified:** build + tests green; AX5 (accessibility-XXXL) screenshot matrix on iPhone 17e/17 Pro/17 Pro Max — no clipping, glyphs scale with their circles, layouts reflow. Grep gate: only `ResultComponents:535` fixed-size placeholder remains (documented exclusion). Commit `cfbcd78`.
- **Gotchas for next time:** (1) no "iPhone SE (3rd gen)" sim installed — use **iPhone 17e** as the small-screen matrix proxy. (2) `simctl launch` needs the **`SIMCTL_CHILD_`** prefix to pass env (e.g. `SIMCTL_CHILD_SHOW_SCREEN=result`) — plain `KEY=val` after the bundle id becomes a launch *arg*, silently ignored → app boots to Home. (3) Build volume filled to 0 B mid-run; `rm -rf ~/Library/Developer/Xcode/DerivedData/*` frees GBs but forces a rebuild.

### 2026-07 · Full design revamp planned: 82-image competitive scan → DESIGN.md v2 → screen specs → MASTER_PLAN_PRE_D
- **Founder directive:** create the complete plan for everything before Phase D, revamp UI/UX for every screen, responsive across all iPhones, enterprise-level design rigor — competitor scan first, then design docs, then screen-by-screen.
- **All 82 reference images analyzed** (5 parallel design agents): 61 competitor shots (Oasis / ChemZero / Food Scanner / Yuka-style), Ivy moodboards, Ingrex composite, branding kits → `reference/competitors/design-teardown.md` — 24-item STEAL list mapped to our screens, 14-item AVOID checklist (now a blocking review gate), opportunity map. Key steals: Oasis trust footer + bordered ingredient cards + named/dated sources; Food Scanner's "estimated because X missing" confidence caveats + methodology sub-pages; ui-screens 6's Negatives/Positives bar meters (→ our "Watch-outs/Benefits"). ChemZero = the anti-pattern library (unsourced dread stats, mortality framing, paywall games).
- **DESIGN.md rewritten (v2 charter):** "Calm Intelligence" position (calm AND dense — no competitor does both), five design laws, responsive system (4 device classes SE→Pro Max, adaptive grids, readable-measure caps, Dynamic Type XXL gate, 6-shot screenshot matrix SE/17 Pro/Pro Max × default/XXL), component canon incl. 3 new components (Watch-outs/Benefits meters, confidence caveat callout, trust footer — last one already built).
- **`docs/SCREEN_SPECS.md` (new):** every screen start-to-end spec'd with current-state audit → changes. Audit verdict: codebase already unusually strong (trust footer, next-action, confidence chips, skeletons, Reduce-Motion, VO announcements all live). Real gaps: **no search/manual barcode entry anywhere** (biggest), swaps = honest stub, fixed icon glyph sizes don't scale with Dynamic Type, off-grid padding literals (20/32), Me profile-load error silent, ResultSkeletonView designed-but-unwired, no pantry item removal.
- **Design skills toolchain added to the plan** (founder request): `ui-ux-pro-max` skill installed at `~/.claude/skills/ui-ux-pro-max` (the `/plugin install` had silently failed — empty marketplace dir; cloned from github.com/nextlevelbuilder/ui-ux-pro-max-skill instead). It's a searchable design DB (SwiftUI do/don't rules, 99 UX guidelines, pre-delivery checklists) — **rules/checklists only; its palette/font suggestions are never adopted (brand locked to V3)**. `/ux-writing` used to draft all new-surface copy → `docs/COPY_DECK.md` v1.1 §New surfaces (search, swaps, compare, report-issue, offline, rate-limit, feedback gate, attribution). Plan's Standing rules now name the full skill gate set (ios-design-review, accessibility-audit, ux-audit, writing-plans).
- **Project skill pack installed (Vercel skills CLI):** `npx skills add jakubkrehel/make-interfaces-feel-better` + `dpearson2699/swift-ios-skills` → 85 skills landed in `.agents/skills/` (symlinked to `.claude/skills/`), **pruned to 34 SafeSide-relevant** (kept SwiftUI/Swift-quality/vision/a11y/networking/simulator/StoreKit-for-Phase-D; removed carplay/gamekit/weatherkit/tabletopkit etc. — each skill description costs session context; re-add via `npx skills add dpearson2699/swift-ios-skills@<name>`). Committed to the repo with `skills-lock.json` so they're shared. Mapped per-chunk in MASTER_PLAN_PRE_D §Skills toolchain.
- **`MASTER_PLAN_PRE_D.md` (new, root):** single ordered plan, chunks 0–8: UI hygiene → Result upgrade (meters + report endpoint) → Search → Swaps engine → data/correctness (current-score view, OCR dupe, chat rate-limit, re-score cron) → Compare v1 → robustness/offline/a11y sweep → analytics + rating gate + legal/ODbL → optional Planner v1 (founder-gated). Each chunk gets its own detailed TDD plan at start; exit criteria + standing rules (matrix screenshots, AVOID-list gate, key rotation before Chunk 7). Phase D stays LAST.

### 2026-07 · Scanner rebuilt (real zoom + shatter), grounded AI chat, USDA live
- **Scanner core replaced: DataScannerViewController → custom AVCaptureSession.** DataScanner has NO zoom API (why zoom "did nothing"). New pipeline: virtual multi-camera (`builtInTripleCamera`/dual → real optical lens switching) + `AVCaptureMetadataOutput` (barcodes) + `AVCapturePhotoOutput` (OCR stills) + `AVCaptureVideoDataOutput` (shatter frame). `cycleZoom` 1×/2×/3× clamped to device max, applied to the exact session device. Session work serialized on a private queue, UI hopped to main.
- **Right-edge control cluster** (zoom / gallery / torch) + instruction pill moved below the reticle (was colliding with nav). **Gallery** = pick a photo → VNDetectBarcodes → live-lookup, else OCR.
- **Scan-success "shatter" animation** (founder request, from a web ref): on lock, the frozen camera frame breaks into a 5×8 grid of shards that fly out + fade 0.45s (Reduce Motion → lime flash). Doesn't gate lookup.
- **Grounded AI chat** (Phase 3, pulled before Phase D): `POST /chat` grounds on the product's DB data + score breakdown + ingredient KB + (RLS) profile; bounded LLM (Groq), banned-word filter, **always "not medical advice"**, sources from data not the model, graceful without a key. iOS `ChatView` sheet from Result ("Ask about this product"). 121 deno tests. Live-verified: "safe for my kid?" → factual + sourced + declines medical verdict. **Legal/FTC review of claim language still required before this ships publicly** (Phase 3 exit criterion).
- **USDA enrichment now LIVE** — `USDA_API_KEY` set as a Supabase secret (key valid; rotate — pasted in chat). Applies on fetch-path scans; score_version 1.1.0 means re-scans re-enrich.
- **Note:** the gitignored `ios/FoodScanner.xcodeproj` keeps getting deleted between build steps by something in the env — always `xcodegen generate` immediately before `xcodebuild`, and confirm it exists, or the build reinstalls a stale binary.

### 2026-07 · UI/UX overhaul (Design System v3) + functionality wave
- **Founder directive locked:** UI/UX is priority #1, never compromised; launch + payment (Sign in with Apple, RevenueCat) go LAST. Build-to-spec + review-on-device; keep+elevate bold-green; references Yuka/Cal AI/Oasis/Ingrex. See [[ui-ux-first-directive]].
- **Design System v3** (`docs/DESIGN_SYSTEM_V3.md`): resolved light-vs-dark → **light-first** (mint canvas #F4F8F1, white flat cards, radius 20, pill CTAs, Space Grotesk display font bundled OFL) + bold-green accent + dark only for the scan moment. Studied the founder's `reference/moodboards` + `reference/screenshots` (Ingrex/Ivy/Oasis). Theme tokens rewritten light-first.
- **Screenshot-verify workflow** (key process fix): a DEBUG harness in FoodScannerApp (`SHOW_SAMPLE_RESULT`, `SHOW_SCREEN=me|plan|onboarding|result`) boots straight into any screen so screens are verified via `xcrun simctl` screenshots on the simulator BEFORE hitting the device — stops the founder seeing broken layouts. To seed a populated sim state: reset sim keychain → capture the fresh anon user id → insert pantry rows via Management API.
- **Screens rebuilt to v3 (all verified on sim):** Home (flat cards, product-card grid, grade dots, scan-CTA anchor), Result (Oasis-level: floating image + circular score RING + trust chips + tri-metric Nutrition/Additives/Processing + sourced "why" + calm color-coded ingredient cards, never alarm-red), Scan (green reticle/torch/pill banners), Onboarding (option cards + slim progress + pill CTAs), Me tab (profile edit + ED-safe settings + about/ODbL), Plan (calm coming-soon). Brand **app icon** (scan brackets + leaf, green-black) added.
- **Home layout fixes** the founder caught: removed card shadows (flat), fixed filter chips wrapping (own row + fixedSize), hid redundant "Home" nav title (killed the ghost), bottom padding to clear the tab bar.
- **Allergen alerts:** scanned product matched against the user's onboarding allergies (literal normalized matching, 7 conservative synonym groups) → calm caution banner + flagged chips + neutral ingredient notes. Never alarm-red block. Health-flag→ingredient mapping deliberately NOT done (would need a medical inference table = fabrication risk).
- **Data quality:** ingredient KB 73→174 (regulator-sourced), 73 new additives added to the scoring table (KB↔scoring tier parity kept), USDA FoodData Central enrichment on thin-data cache-misses (OFF precedence). **score_version 1.0.0→1.1.0** (additive table changed → cached products re-score; calibration scores unchanged). 97 deno tests. Deployed + KB seed applied (174 rows live). USDA_API_KEY not set yet (graceful OFF-only).

### 2026-07 · Phase 2 wave 2: OCR fallback, favorites, trending, next-action, additives fix
- **4 parallel agents, strict file ownership + a pinned favorites API contract** (`isFavorite(_:)` / `toggleFavorite(productID:) async`) shared verbatim between the result-page and home agents so they interlocked. `ProductView(product:)` init frozen so no agent broke others' call sites. All 4 merged and compiled clean first try; on device.
- **Additives "bug" was a stale deployment**, not code — the mapping already existed. Redeployed `ingredients`; Coca-Cola now surfaces E150d/E338 with sources + risk tier. Added prefix-tolerance (`e338` == `en:e338`) + 6 regression tests (78 total).
- **On-device OCR shipped:** `needsOCR` → "Snap the label" → `DataScannerViewController.capturePhoto()` (verified against the SDK swiftinterface — reuses the scanner's own session, avoids a second contending AVCaptureSession) → `VNRecognizeTextRequest .accurate` → `POST /product/ocr`. New phases `.capturingLabel` / `.labelNotFound`; lighting-fail vs network-fail get different honest copy.
- **Favorites:** optimistic in-memory `Set` (sync `isFavorite`), status flips scanned↔favorited; works on never-scanned products (trending cards). Home has Recent/Favorites filter + heart on rows/cards.
- **Trending healthy:** top `band=high` products, horizontal cards. KNOWN LIMITATION: takes highest-ever high score per product from a fetched window (score_results keeps history; PG can't do top-1-per-group in one call) — proper fix is a backend view exposing the current score per product.
- **Next-action** is honest: no fabricated swaps (engine is Phase 3) — a band-specific tip + real "scan another" CTA. Copy is new, needs a COPY_DECK pass.
- Follow-ups noted: ingredient-load error needs manual retry (auto-retry?); OCR parenthetical `colour (E150d)` yields a redundant orphan "Colour" entry; trending current-score view.

### 2026-07 · Phase 2 wave 1: pantry, onboarding, AI ingredient KB, OCR (deployed + on device)
- **Four features shipped in parallel** (2 backend + 2 iOS agents): pantry auto-save, Home recent-scans, skippable onboarding, AI ingredient explanations, OCR endpoint. All committed; app rebuilt + installed on the iPhone 13.
- **AI ingredient KB is live and guardrailed.** `ingredient_kb` (73 curated entries: 51 additives from the scoring table + 22 common ingredients, every claim regulator-sourced) + `ingredient_explanations` cache. `GET /product/:id/ingredients`: KB retrieval → cache → bounded LLM rewrite of **only** the what/whyUsed/safety prose; riskTier/sources/etc are verbatim from the KB so the LLM can't move risk. KB miss → "no vetted info yet" (never fabricates). 72 deno tests incl. all 5 §7 guardrails. Live-verified: "Sugar" → WHO/USDA-sourced explanation; unknown token → limited state.
- **LLM is provider-agnostic** (OpenAI-compatible). Using **Groq** (`llama-3.3-70b-versatile`) via free key — stored as Supabase secrets `LLM_API_KEY/LLM_BASE_URL/LLM_MODEL`, never in repo. Google AI Studio key the founder gave was an `AQ.`-prefixed OAuth token, not a usable `AIza` Gemini API key — rejected. **Both pasted keys must be rotated** (chat logs persist).
- **KNOWN GAP (follow-up):** the ingredients endpoint explains `ingredients_text` tokens but does NOT yet map `additives_tags` (e.g. `en:e150d`, `en:e338`) to their KB entries, so additives with real KB entries don't surface on the result screen. Pipeline is otherwise sound. Fix next.
- **OCR endpoint** `POST /product/ocr`: label text (on-device Vision does the OCR) → parse → same scoring engine (always unknown/limited, honest) → provisional `source=ocr` product. iOS Vision capture not wired yet.
- **Cache seeded** with 10 real barcodes across the spectrum (28–97) for demo/instant re-scan.
- **Pantry detail is thin:** pantry-tapped products show score but empty "why"/ingredients (list read selects few columns); needs a re-fetch-by-id later. `save()` is update-then-insert (not upsert) to preserve `first_scanned_at`.
- **Test note:** full `xcodebuild test` (incl. XCUITest) times out ~10min on sim boot; run `-only-testing:FoodScannerTests` for fast unit runs (5 pass).

### 2026-07 · First vertical slice LIVE (backend deployed + iOS builds/tests green)
- **Backend deployed to Supabase** (`usmdthxnxzdywtjgbokl`): schema migration applied via Management API (5 tables, RLS verified), anonymous sign-ins enabled, `product` Edge Function deployed. **Live end-to-end test passed:** anonymous JWT → `GET /functions/v1/product/3017624010701` → Nutella scored 38/"low" with sourced factor breakdown; cached row + score persisted; second call = cache hit.
- **Real-world data gap surfaced:** OFF currently returns no `nova_group` and empty `additives_tags` for Nutella — the renormalization path (nutrition 0.70 / additives 0.30, "limited" confidence) fired in production on scan #1. Validates the design; also confirms USDA enrichment + OFF data-quality handling matter early.
- **iOS builds + all tests green** on iPhone 17 Pro simulator (Xcode 26.6, supabase-swift 2.50.0, RevenueCat 5.80.2). Two first-build fixes: (1) VisionKit `DataScannerViewController.isSupported` is MainActor-isolated → `CameraAvailability.current` marked `@MainActor`, availability resolved `.onAppear` (`@State` defaults evaluate off-main); (2) test targets need `GENERATE_INFOPLIST_FILE: YES` in project.yml or signing fails.
- **SPM gotcha:** killing xcodebuild mid-resolve corrupts the package cache ("Couldn't get the list of tags: fatal: not a git repository") → delete the project's DerivedData and re-resolve.
- Secrets: Supabase URL/anon key in `ios/Config.xcconfig` (gitignored) + `~/.env`; access token (30-day expiry) in `~/.env`. Nothing in the repo.
- Remaining for milestone: run on a physical iPhone (needs device + team in Xcode) + real camera scan.

### 2026-07 · Build started: repo, fixtures, scoring rule locked, parallel tracks
- **Git repo initialized** (was folder-only). `.gitignore` excludes secrets (`.env*`), Xcode user state, generated `.xcodeproj` (regenerated via XcodeGen).
- **Calibration extracted to fixtures:** `tools/extract_calibration.py` (stdlib-only) parses `docs/Scoring_Calibration.xlsx` → `supabase/functions/_shared/scoring/{weights,calibration}.json`. Verified the formula reproduces **all 50 expected scores exactly**.
- **Scoring rounding rule locked:** final score uses **half-up rounding** (96.5→97), matching Excel `ROUND`. Python's banker's rounding gives off-by-one on *.5 values — don't use it. Additive penalties count first/additional **per tier** (e.g. higher+moderate = −15−6 = 79).
- **Scoring-engine test conflict resolved (founder OK'd):** KICKOFF said Swift Testing for the engine, but BACKEND_SPEC puts the engine server-side (TypeScript/Deno Edge Function). Decision: **engine tests in Deno** against the calibration fixtures; Swift Testing covers client model decoding + band mapping.
- **Xcode project via XcodeGen** (`ios/project.yml`) instead of a committed binary `.xcodeproj` — reviewable in git, regenerable. Xcode itself not yet installed on this Mac (founder installing); all non-Xcode work proceeds in parallel.
- Tooling installed via brew: deno 2.9.1, supabase CLI 2.109.1, xcodegen 2.45.4.
- Unknown additives (not in our curated risk table) score as **low tier but labeled "unknown"** in the breakdown — we never invent risk (spec §5 governance).

### 2026-06 · Apple HIG made an explicit principle
- Made "native-first, brand-skinned (Apple HIG)" a first-class design principle (`DESIGN_SYSTEM.md` §1 #5) with a concrete defer-vs-override table (§11.1) and a `DESIGN.md` foundation note. Previously HIG alignment was present but implicit (SF Pro, SF Symbols, Dynamic Type, VoiceOver, native SwiftUI components). Rule: defer to Apple for behavior/navigation/gestures/accessibility; brand overrides color/type/Score Badge/spacing; on conflict, HIG wins behavior, brand wins visuals.

### 2026-06 · iOS-only reconfirmed; Android = explicit future
- Founder reaffirmed **iOS-only, SwiftUI now; Android later (after iOS PMF)**. Captured explicitly in `MASTER_PLAN.md` (principle 1 + Roadmap → Future) so it's a planned future item, not an open question. No Android work in any current phase.
- Recheck done: core plan/specs/scaffold are cleanly iOS-only. Cleaned residual mentions: agents README now flags "ignore the agents' Android/Material guidance — iOS-only"; DESIGN_SYSTEM font note de-emphasized cross-platform. Master_Build_Plan.docx stays bannered/superseded.
- Added a "Current state & next action" snapshot at the top of MASTER_PLAN.

### 2026-06 · Build-readiness gaps closed + Phase 0 scaffold started
- Added `docs/BACKEND_SPEC.md` (Supabase Edge Functions + product-data pipeline + endpoints + cost/observability) and `docs/AI_INGREDIENT_EXPLANATION.md` (curated ingredient KB → retrieval → LLM rewrites, never invents; the core credibility feature).
- Calibrated scoring: `docs/Scoring_Calibration.xlsx` (50 products, tunable weights, edge cases pass) + `tools/off_live.py` to pull real OFF data by barcode (run locally). Inputs in the workbook are representative pending a live pull.
- Scaffolded the iOS app: `ios/FoodScanner/` (SwiftUI — app entry, Theme tokens, Models, guest-first SessionService, APIClient, ScoreBadge, Home/Scanner/Product views) + `ios/README.md`. Not an .xcodeproj — create in Xcode, add SPM deps, run on device.
- Verdict: ready to build the MVP. Remaining pre-ship items are already scheduled in Phase 4 (legal, assets, a11y). Validation (Phase 1) still recommended in parallel.

### 2026-06 · Auth: guest-first, optional sign-in
- No login wall. App runs on an anonymous session from first launch (Supabase anonymous auth or local); onboarding + scanning + pantry all work with no account.
- Sign in with Apple is offered later as a benefit ("save/sync across devices") and links the anonymous account without data loss. No Google sign-in in MVP. Account deletion required when sign-in ships (Apple rule).
- Rationale: auth before value kills activation and would confound Phase 1 validation. Add real auth only when there's a reason (cross-device sync, scaled personalization). Reflected in `MASTER_PLAN.md` (principle 7 + Phase 2) and `docs/product-requirements.md` (Epic E).

### 2026-06 · Master Plan v3 (phase-wise) + scope reset
- Rewrote the master plan as `MASTER_PLAN.md` (root), phase-wise, native iOS, incorporating the founder's "AI consumer-health platform, food-first" vision.
- Platform: imported founder context specifies SwiftUI → confirms **native iOS, iOS-only** (closes the open Android question unless explicitly reopened).
- **MVP scope sharpened:** true MVP = scan → AI ingredient explanation + deterministic verdict + one next action + pantry. Comparison, AI chat, alternatives, search moved to Phase 3. Diet planner is **not** in the food MVP (revisit post-PMF).
- **Rejected the "150–200 docs before building" idea** as analysis paralysis; right-sized to ~30 decision-driving docs (most already exist).
- Restored ED-safe design + health-claim guardrails, which the imported context had dropped.
- Highest-value pre-build task reaffirmed: calibrate the scoring model on ~50 real products.

### 2026-06 · Platform DECIDED: native iOS only (Swift/SwiftUI)
- Founder chose to build **iOS-only, pure native Swift** for now — superseding the earlier React Native/Expo recommendation and the smooth-app (Flutter) fork option. No Android.
- Verified native stack in `docs/NATIVE_IOS_STACK.md`: VisionKit DataScanner (barcode) + Vision OCR (both first-party, a win for a scanner), URLSession+Codable for OFF/USDA REST, RevenueCat purchases-ios (MIT) or StoreKit 2, supabase-swift (MIT), SwiftUI `@Observable`, Swift Testing + XCUITest + swift-snapshot-testing.
- Backend unchanged (scoring, OR-Tools optimizer, AI route stay server-side; iOS is a thin client). The `ios-app-developer` agent now applies directly.
- Trade-off accepted: no Android path, nothing to fork (smooth-app was Flutter) — more UI built from scratch, but better native scanning/OCR and platform fit.

### 2026-06 · Added specialist agents (expertise)
- Placed 5 Claude Code subagents in `.claude/agents/` (ux-researcher, ux-product-designer, ui-designer, app-ui-designer, ios-app-developer) + a screensdesign scraper in `.claude/tools/`. Indexed in `.claude/agents/README.md`; referenced from `CLAUDE.md`. Use them during build, routed research → product → UI → iOS. ios-app-developer assumes native Swift — which is now the decided stack, so it applies directly.

### 2026-06 · Brand direction: bold green (ChemZero-inspired)
- Founder chose a vivid green / green-black identity with a lime accent (ref: ChemZero, Zentrix brand kits in `reference/moodboards/branding/`). `DESIGN_SYSTEM.md` updated to v2.0 with the green palette + a full Brand Kit section; `DESIGN.md` synced.
- **Guardrail kept:** the brand is bold, but food **scores remain non-alarmist** (brand-green/amber/clay, never "toxic" red) per the ED-safe principle in CLAUDE.md. Lime is an accent on dark surfaces only (contrast).
- Note (my flag, founder's call): bold lime risks reading like the ChemZero alarmist app we position against — recommend validating the palette reads as *trustworthy* with the Skeptic/Parent personas before launch.

### 2026-06 · Adopt structured project layout
- Reorganized the repo into `CLAUDE.md` / `DESIGN.md` / `MEMORY.md` + `docs/` + `reference/`. Skipped team-only Claude Code scaffolding (agents/, settings.local.json) and `.mcp.json` until needed (solo founder, pre-build).

### 2026-06 · Product thesis locked
- Building a food scanner **and** diet planner as **one connected loop**, not two features. The Pantry (auto-filled by scans) is the bridge object.
- **Wedge:** transparency + calm/non-shaming tone + honest pricing + verdict→action. Competitors are distrusted and judgmental.

### 2026-06 · Tech direction (SUPERSEDED — see native-iOS entry above)
- ~~**Recommended:** React Native / Expo path (obytes template).~~ Superseded by the native-iOS Swift decision.
- ~~**Fast alternative:** fork `openfoodfacts/smooth-app` (Flutter).~~ Not applicable (native iOS).
- Data: Open Food Facts (ODbL — attribute + share-alike) + USDA FoodData Central (public domain). Scoring engine is our own code and is reused by scanner + planner.
- **Avoid copying** GPL/AGPL apps (OpenNutriTracker, wger, Mealie, Tandoor) into the closed app — reference only.

### 2026-06 · AI planner rule
- The LLM **selects and explains only**; it may not invent products or compute nutrition. All numbers come from the DB; optional OR-Tools/PuLP solver for hard constraints.

### 2026-06 · Trust & safety stance
- ED-safe design is a first-class requirement: qualitative framing by default, calories opt-in, no good/bad/toxic language, no streaks/shaming, soft off-ramp to support.
- Honest billing: transparent price, real 7-day trial, one-tap cancel. Free = scan + score; Pro = planner + swaps + AI.

### 2026-06 · Build sequencing
- Phase 0 foundation → 1 scanner MVP → 2 manual planner → 3 AI + connected loop → 4 harden/test/ship iOS. Each phase ships something usable; testing built into every phase.

## Open questions (validate with primary research)
- Are the persona segment sizes (~35/30/25/10) accurate? → run the survey.
- Will users connect scan↔plan on their own, or must we teach it? → usability test the handoff.
- Does the transparent score actually read as credible to skeptics? → prototype test.
- Real willingness-to-pay at honest pricing? → survey + beta.
