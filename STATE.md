# STATE — resume brief for SafeSide

**Read this first to continue work.** Pairs with `MEMORY.md` (full decisions log, newest at top), `CLAUDE.md` (principles), `MASTER_PLAN.md` (phases), `docs/DESIGN_SYSTEM_V3.md` (the live UI system). Last updated: 2026-07 (native iOS build well underway).

> App name: **SafeSide** (display name; internal Xcode target/bundle id still `FoodScanner` / `io.omnisai.foodscanner` — don't rename those, it breaks signing).

---

## What SafeSide is
Native **iOS** (Swift/SwiftUI, iOS 17+) food-ingredient scanner: scan a barcode → deterministic, sourced health/processing score + transparent "why" + AI ingredient explanations + grounded AI chat. Guest-first (anonymous Supabase auth, no login wall). ED-safe, non-alarmist. Backend = Supabase (Postgres + Edge Functions). See `CLAUDE.md` for the non-negotiable principles.

## Status: MVP + Phase-3 depth largely BUILT and running on a physical iPhone
**Working end-to-end (deployed + on device):**
- Guest anonymous auth; **onboarding** (8Q skippable, ED-safe); **Home** (scan CTA, recent-scans grid, trending); **Scanner** (AVFoundation, real 1×/2×/3× optical zoom, torch, gallery photo-scan, OCR label fallback, scan-success "shatter" animation); **Result** screen (circular score ring, tri-metric Nutrition/Additives/Processing, sourced "why this score", calm color-coded ingredient cards, allergen alerts); **AI ingredient explanations** (KB + LLM, guardrailed); **grounded AI chat** ("Ask about this product"); **pantry** auto-save + **favorites**; **Me** tab (profile edit, ED-safe settings) + **Plan** placeholder; brand **app icon**.
- Backend: Postgres schema + RLS, deterministic **scoring engine** (calibrated to 50 products, `score_version 1.1.0`), **174-entry ingredient KB** (regulator-sourced), OFF client, **USDA enrichment**, endpoints `GET /product/:barcode`, `POST /product/ocr`, `GET /product/:id/ingredients`, `POST /chat`. **121 Deno tests green.** Design system v3 (light-first, bold-green, Space Grotesk) applied across all screens.

## Revamp progress
- **Home redesign (Variant C + how-it-works carousel) — BUILT & TESTS GREEN (2026-07-11). iOS-only, no backend.** Home's top restructured: **wordmark+greeting** ("SafeSide" Space-Grotesk `greenDeep` + "Know what's really in your food.") → **swipeable how-it-works carousel** (paged `TabView` + custom brand dots [active `green`/inactive `greenSoft`]; 3 `greenDeep` cards Scan→sourced score→better swap; **slow 7s auto-advance that no-ops under Reduce Motion**; `@ScaledMetric` card height; **4th "featured product" slide scaffolded-commented** `// TODO(home-later)` — no fake pick) → **quick-action grid** (4 `greenSoft` tiles, reflow 4-across→2×2 at AX type): **Scan**→scanner cover · **Search**→push `SearchView` · **Categories**→switch tab via new `RootTab` enum + `HomeView(onSelectTab:)` (avoids nesting CategoriesView's own `NavigationStack`) · **Favorites** (NOT "Compare" — no 2-product picker exists; brief-sanctioned)→flip pantry filter + `ScrollViewReader` scroll → **Recent scans** + **Trending** (both unchanged incl. all states). Removed old `ScanCTACard`/`SearchFieldRow` (routes moved to tiles); kept `DailyInsightTile`, filter chips/sort/context-remove, offline banner, skeletons; **added `.refreshable` pull-to-refresh**. **Audit fixes folded (Home):** v3 **floating-card soft shadow added to `surfaceCard`** (app-wide lift), "clear, sourced score" copy dup resolved by restructure, `.monospacedDigit()` on grade dot + insight count. Wordmark uses `greenDeep` for AA large-text contrast. Files: `HomeView.swift`, `FoodScannerApp.swift`, `DesignKit.swift`, `docs/COPY_DECK.md` (§Home). iOS **BUILD + 61 tests SUCCEEDED**, no new warnings. Sim-verified 17 Pro (`SHOW_SCREEN=home`): carousel pages 1 & 3, greenSoft tiles, floating trending cards, calm empty recent. Shots: `reference/screenshots/home-redesign/home-page{1,2}.png`. **Thin/deviated:** featured slide absent (scaffolded), Compare=Favorites, Categories tile inert only in the `onSelectTab==nil` DEBUG harness. **Next: pre-Phase-D chain complete — Chunk 8 (Planner) or Phase D (founder's call).**
- **Categories feature — BUILT & TESTS GREEN (2026-07-11). iOS-only, NO new backend.** New bottom-nav **Categories** tab (bar = **Home · Categories · Scan · Me**; Plan removed from the bar but `PlanView` stays in code + `SHOW_SCREEN=plan` harness, `// TODO(categories-later): restore Plan tab`). `CategoriesView` (curated `FoodCategory.all` cards, Oasis `categories-01`, SF-Symbol green tiles) → `CategoryDetailView` ("Top rated · {category}", Oasis `list-01`) ranked **best-first by OUR own sourced score** via pure `CategoryService.rank` (drop `confidence=="limited"` + non-current-`SCORE_VERSION`, dedup, score-desc, cap 50). Reads `products` ⋈ `product_current_scores` directly (anon-readable, same as `loadTrending`); guards on client presence + keys `.task` on `session.userID` so it loads during guest bootstrap. **HONESTY:** NO "Lab tested"/"Proven by N agencies" badge and NO "Unlock top rated" paywall (both in Oasis's ref — deliberately not replicated; we hold no lab data — principle #1 + AVOID). `verifiedBy: [Agency]` field + `VerifiedByBadge` **scaffolded but always empty/off** (`// TODO(categories-later): populate from a real dataset; NEVER hardcode agencies`). Honest empty state for thin categories (2–8 products/category today). New files `FoodCategory.swift`/`CategoryService.swift`/`CategoriesView.swift`/`CategoryDetailView.swift` + `FoodCategoryTests`/`CategoryRankingTests`; edited `FoodScannerApp.swift` (tab swap + `SHOW_SCREEN=categories|categorydetail`). iOS **BUILD + 61 tests SUCCEEDED** (+14; no new warnings). Sim-verified 17 Pro: categories list clean; Sodas & colas ranked Zero 41 / Zero 41 / Coke 28 / Coke 28 with thumbnails + Scored chips + compact rings, zero lab/agency wording. Commented-for-later (scaffolded + tagged): filter chips, paywall gating, card image thumbnails, count badges, /categories edge fn, dynamic discovery, verifiedBy badge, Plan-tab restore, hide-empty filter. Plan: `docs/superpowers/plans/2026-07-11-categories-feature.md`. **Next: pre-Phase-D chain complete — Chunk 8 (Planner) or Phase D (founder's call).**
- **Chunk 7 (Analytics + feedback + legal surfaces) — BUILT & TESTS GREEN (2026-07-11). LAST pre-Phase-D chunk (Chunk 8 Planner deferred).** Client-heavy + one pure-SQL migration. (1) **Batched best-effort analytics** — new `AnalyticsLogger` (`@Observable @MainActor`; buffers, flushes at 10 + on `scenePhase→.background`, bounded re-buffer at 200, never throws/blocks UI) inserts **directly via PostgREST** into the existing owner-only `events` table (**NO edge function**). Canonical event names = **ANALYTICS_METRICS §4** (not the plan shorthand): `scan_started/succeeded/failed`, `score_viewed`, `why_score_expanded`, `chat_opened`, `swap_shown` (+`swap_accepted` TODO(chunk-3)), `data_reported`, `feedback_sentiment`, `app_review_requested`. Wired at real call-sites (ScannerView, ProductView, ResultComponents CollapsibleSection→WhyScoreSection, ReportIssueSheet submit-success). (2) **PII guard = a type + a test:** `AnalyticsValue` (only `.string/.int/.double/.bool`) makes free text structurally unencodable into `events.props`; the sentiment gate's message goes ONLY to the new owner-only append-only `app_feedback` table. (3) **Sentiment gate** (`FeedbackGate`+`FeedbackGateSheet`) fires **once after the 3rd successful scan, never in onboarding**, equal-weight dismiss, latches so it never nags — unhappy→`app_feedback`+`feedback_sentiment`, happy→`requestReview()`+`app_review_requested`. (4) **Legal:** new `LegalViews.swift` — Privacy (discloses analytics), Terms, OFF/ODbL+USDA attribution — on DESIGN_SYSTEM_V3 tokens; Me About rows relabeled+rerouted; placeholder sheets deleted. Migration `20260710120000_events_hardening_feedback.sql` (events index+len CHECK, `app_feedback` table+RLS). iOS **BUILD + 47 tests SUCCEEDED** (+10: 6 AnalyticsLogger, 4 FeedbackGate; no new warnings); `deno task test` reconfirmed **212 passed** (backend untouched). Sim-verified (17 Pro, default+AX5): Privacy/Terms/Attribution/Me-rows/FeedbackGate — clean, chips reflow vertical at AX5. New DEBUG harness: `SHOW_SCREEN=privacy|terms|attribution|feedback`. **⚠️ LEGAL REVIEW REQUIRED** — Privacy/Terms bodies are DRAFTS in the deck's voice (in COPY_DECK §Legal bodies), founder/legal sign-off is a ship gate; contact emails are placeholders. **NOT deployed — DEPLOY GATES:** rotate keys FIRST (Cerebras/Groq/USDA + Supabase access token) → apply migration `20260710120000` (+ still-unshipped Chunk 1–4 migrations/functions) → RLS psql proof (`events`+`app_feedback`, gate-only) → founder/legal review of legal bodies → device run (events land with correct names + session `user_id` + no PII in props; gate only after 3rd scan, never onboarding; "Not great" writes `app_feedback`, not `events`). Plan: `docs/superpowers/plans/2026-07-10-chunk-7-analytics-feedback-legal.md`. **Next: pre-Phase-D chain complete — Chunk 8 (Planner) is founder's call; else Phase D (Sign in with Apple, paywall, App Store).**
- **Chunk 6 (Robustness / offline / a11y sweep) — BUILT & TESTS GREEN (2026-07-10). iOS-only, no backend.** Hardening sweep: (1) **first-class offline** — new `APIError.offline` via pure `mapURLError` (distinct from `.transport`), new `@Observable NetworkMonitor` (`NWPathMonitor`, injected app-wide, informs UI only — never gates), new reusable neutral-gray `OfflineBanner` (Home shows it above the pantry; cached grid still wins). (2) **Full 429 client path** (Chunk 6 owns it) — `APIError.rateLimited(retryAfterSeconds:)` parsed from the 429 body **and** `Retry-After` header (pure `parseRetryAfter`); `ChatViewModel` disables input+Send+Retry for a per-second countdown with the calm COPY_DECK line, auto-re-enabling. (3) **Every "Something went wrong" gone** (grep = 0) — one new COPY_DECK §Errors string "That didn't load right. Give it a moment and try again." + offline-specific scan/chat/ingredient variants. (4) **Shatter-orientation edge** — pure `shatterImageOrientation(connectionApplied:)` (TDD) stamps the snapshot upright even when the video connection wasn't rotated. (5) **Portrait lock** explicit in `project.yml` (iPhone portrait-only; iPad both portrait variants) — confirmed in generated plist. (6) **Deliverable:** `docs/TEST_PLAN.md` **§6** per-screen pass/fail checklist. iOS **BUILD + 37 tests SUCCEEDED** (+3 new suites; no new warnings); `deno task test` reconfirmed **212 passed** (backend untouched); Result sim-verified default+AX5 (no clipping). **Remaining GATE-ONLY (device):** VoiceOver swipe, real airplane-mode script, camera shatter-orientation visual, rotation crash-free walk, full AX5 walk. **0 AA contrast FAILs filed.** Plan: `docs/superpowers/plans/2026-07-10-chunk-6-robustness-offline-a11y.md`. **Next: Chunk 7 (analytics/legal).**
- **Chunk 5 (Compare v1) — BUILT & TESTS GREEN (2026-07-10). iOS-only, no backend.** New `CompareView.swift`: two already-scored products side by side — two-column header (image + name/brand + reused compact `ScoreBadge`), aligned metric rows (Overall + Nutrition + Additives + Processing) with both products' meters on ONE fixed **0–100 shared scale**, a **subtle per-row winner wash** on the winning side only (its own band colour — amber for a mid winner, never green-under-low, never red), and "Pick this one" → `save(product:)` → "Saved to pantry." → dismiss (never a dead-end). Pure `ComparePair` winner logic in `Models.swift` (unknown side never wins; fixed 4-row order) + `CompareLogicTests` (8 cases). New score-meter primitive `SharedScaleMeter` (sibling to Chunk 1's `MeterRowView`, which is nutrient-driven — NOT a duplicate) + extracted single `ScoreBand.tint` colour source adopted across `ScoreBadge`/`TriMetricTile`/`ScoreFactorRow`. Compact/AX reflow = sticky A/B segmented `Picker` (names truncated 18 chars) stacking one card at a time. Entry: Result-overflow `ellipsis.circle` menu → partner-picker sheet (recent pantry scans) → `CompareView`; **Search multi-select entry DEFERRED** (clean `TODO(chunk-5 follow-up)`). `deno task test` reconfirmed **212 passed** (backend untouched); iOS BUILD + TEST SUCCEEDED (21 tests, no new warnings). Screenshot matrix (17e/17pro/promax × default/AX5) clean: aligned rows, subtle tint, no clipping, compact toggle engages. COPY_DECK §Compare got 1 new neutral string ("Higher"). Plan: `docs/superpowers/plans/2026-07-10-chunk-5-compare-v1.md`. **Note for Chunk 6:** a pantry-picked partner is thin (`asProduct()` → `factors:[]`) so its factor rows honestly show dashed/tie while Overall still compares; a11y reads aligned rows ("{label}. {A} n of 100. {B} n of 100. {winner} scores higher on {metric}"). **Next: Chunk 6 (robustness / a11y sweep).**
- **Chunk 4 (Data & correctness wave) — BUILT & TESTS GREEN, deploy pending (2026-07-10).** New **`product_current_scores`** view (`security_invoker`, one row/product = latest by `computed_at`) fixes the highest-ever-score bug and is now the single source of truth for trending/swaps/search — both `// TODO(chunk-4)` markers (`search/index.ts`, `swaps/index.ts`) resolved to read it; iOS `PantryService.loadTrending`/`loadRecent` read the view (band-high + score-desc order kept, dedup now a no-op). **OCR** drops the redundant additive-class orphan (`"Colour (E150d)"` → no stray "Colour"; keeps `"Citric Acid (E330)"`; handles comma-in-parens `"Colour (E150c, E150d)"`). **`/chat` rate-limit**: pure sliding-window `withinRateLimit` + service-role-only `chat_rate_events` ledger → **429** + `Retry-After` + the calm COPY_DECK line (per JWT `sub`, anon included); iOS `APIError.rateLimited` renders it in the chat ErrorBubble. **Re-score cron**: new secret-gated (`X-Rescore-Secret`/`RESCORE_SECRET`) `rescore` edge fn re-runs the deterministic engine over stale-`score_version` products from stored `raw_off`, inserts fresh `score_results` (history kept); scheduled via pg_cron+pg_net daily 04:00 UTC. **Dietitian export**: `docs/reviews/SCORE_WEIGHTS_REVIEW_2026-07.md`(+`.csv`) — weights, 124 additive tiers, 50-product calibration, sign-off (no weight change; `score_version` still `1.1.0`). `deno task test` **212 passed** (+5 OCR, +6 chat, +7 rescore); iOS BUILD + TEST SUCCEEDED. **NOT deployed** — need migrations `20260710020000/020100/020200` applied + `functions deploy chat rescore` + `secrets set RESCORE_SECRET` + `app.rescore_secret` DB setting (out of repo). Gate SQL cross-check: `product_current_scores` for a re-scored product == its `MAX(computed_at)` row (newer/lower), NOT `MAX(score)`. Plan: `docs/superpowers/plans/2026-07-10-chunk-4-data-correctness.md`. **Next: Chunk 5 (Compare).** Chunk 6 owns the full 429 client path (respect `Retry-After`); Chunk 7 an optional `chat_rate_limited` analytics event.
- **Chunk 3 (Swaps engine — "See a better option") — BUILT & TESTS GREEN, deploy pending (2026-07-10).** New `swaps` edge fn (`GET swaps/product/:id/swaps`): pure `rankSwaps`/`whyBetter` (same OFF category via `categories_tags` `&&` → strictly-higher current-`SCORE_VERSION` score → allergen-safe against the caller's `profiles.allergies` under RLS → pantry-first stable sort, cap 5, honest `thin`/empty), deterministic **DB-diff** why-better (`"No colours E150d · lower saturated fat"`, no LLM). Service-role reads the global `products`/`score_results`; user-scoped (JWT) client reads pantry + allergies. Schema: migration adds `products.categories_tags` (+GIN) mapped in `_shared/off.ts`; seed `20260710010000_swaps_demo_seed.sql` writes a chocolate-spreads cohort so a low-score demo returns real options. iOS: `SwapCandidate`/`SwapsResponse` + `APIClient.swaps`, new `SwapsView` sheet (ranked cards, mini `ScoreBadge` ring via new compact init, band-tinted delta chip never red, sourced why-better, Save-to-pantry via `PantryService.saveToPantry`, honest empty state), CTA rewired from the retired `NextActionSheet` stub. `deno task test` **195 passed** (+3 off, +16 swaps); iOS BUILD + TEST SUCCEEDED (new `SwapsDecodingTests`). COPY_DECK §Swaps got the 1 missing error string. **NOT deployed** — need `supabase db push` (categories migration + demo seed) + `supabase functions deploy swaps`, then curl-smoke (subject `aaaaaaaa-0000-4000-8000-000000000001` → ≥1 sourced swap; milk-allergy profile filters the milk candidate) + device install + 6-shot matrix + founder review. Plan: `docs/superpowers/plans/2026-07-10-chunk-3-swaps-engine.md`. **Next: Chunk 4 (current-score view — the `// TODO(chunk-4)` markers in `swaps/index.ts` + `search/index.ts`).**
- **Chunk 2 (Search + manual barcode) — BUILT & TESTS GREEN, deploy pending (2026-07-10).** New `search` edge fn over OFF v2 (`_shared/off.ts` refactored: shared `mapOffFields` + `fetchOffSearch`/`mapOffSearchPayload`, kept general for Chunk 3 Swaps) attaching ONLY our current-`SCORE_VERSION` cached score (never OFF's Nutri-Score; unscored → "Not scored yet"). iOS: `SearchResult` model + `APIClient.search` (URLComponents, not the path helper), `SearchView` (name/barcode field, 300 ms debounce, `SearchQuery.classify` regex `^\d{6,14}$`), `ProductLoaderView` (barcode/row → identical scored `/product/:barcode` path, real `ResultSkeletonView` now production-live, never a dead-end), Home search-field entry, and "Enter barcode manually" in the Scan banners (sheet). `deno task test` **176 passed**; iOS BUILD + TEST SUCCEEDED (new `SearchLogicTests`). COPY_DECK §Search got the 3 missing strings. **NOT deployed** — need `supabase functions deploy search` (+ still the Chunk 1 deploy). Then curl-smoke + device install + 6-shot matrix + founder review. Plan: `docs/superpowers/plans/2026-07-10-chunk-2-search-manual-barcode.md`. **Next: Chunk 3 (Swaps).**
- **Chunk 1 (Result upgrade) — BUILT & TESTS GREEN, deploy pending (2026-07-10).** Watch-outs/Benefits bar-meters computed server-side (`_shared/scoring/meters.ts`, FSA + Nutri-Score thresholds) on `score.highlights` (serves on cache + fresh paths, no re-score); additive INS-class category pills (`_shared/additives/category.ts`); harmful/beneficial pre-read counts; named+dated source rows (`fetchedAt`); `ResultSkeletonView` wired (dormant `isResolving`, Chunk 2 flips on); real Report-an-issue round-trip (`product_reports` table + `product-report` fn + iOS submit/thanks/Retry sheet). `deno task test` **157 passed**; iOS BUILD + TEST SUCCEEDED (new `HighlightsDecodingTests`). COPY_DECK §Result upgrades got severity words + INS pill labels + tier-word ladder. **NOT deployed** — still need `supabase db push` (migration `20260710000000`) + `supabase functions deploy product-report product ingredients`, then device install + 6-shot matrix + founder review. Plan: `docs/superpowers/plans/2026-07-10-chunk-1-result-upgrade.md`. **Next: Chunk 2 (Search).**
- **Chunk 0 (UI hygiene) — DONE & VERIFIED (2026-07-10).** Space.s45=20 token + padding literals fixed; 4 banned "Something went wrong" strings → honest copy; `@ScaledMetric` glyphs across 9 sites (ScanCTA, InsightTile, GradeDot, hearts, OptionCard, Me avatar, Plan icon; ProductView nav-heart → semantic `.body`); Me profile load-error retry row; Onboarding allergen subline + "change anytime in Me" note; `PantryService.remove()` + context-menu remove-with-confirm + sort menu (recent/score). Build + tests green; AX5 screenshot matrix clean. Commit `cfbcd78`. Plan: `docs/superpowers/plans/2026-07-09-chunk-0-ui-hygiene.md`. **Next: Chunk 1 (Result upgrade).**
  - Sim note: no "iPhone SE (3rd gen)" installed — used **iPhone 17e** as small-screen proxy for the matrix.

## What's LEFT (do next)
**→ `MASTER_PLAN_PRE_D.md` is now the single ordered plan for everything before Phase D** (chunks 0–8: UI hygiene → Result upgrade → Search → Swaps → data fixes → Compare → robustness → analytics/legal → optional Planner). Design authority: `DESIGN.md` v2 + `docs/SCREEN_SPECS.md` + `reference/competitors/design-teardown.md` (82-image competitive scan, July 2026). The A–E list below is the raw inventory that plan absorbs:
**A. Complete the loop:** real "See a better option" swaps engine (currently honest stub) · wire report-issue endpoint (`POST /product/:id/report`) · analytics events logging · search · comparison.
**B. Data/correctness:** trending current-score DB view (uses highest-ever now) · OCR redundant parenthetical entry · dietitian weight review · rate-limit `/chat` · re-score cron on version bump.
**C. Robustness:** empty/loading/error pass · full a11y + Dynamic Type XXL sweep on device · offline handling · camera video-frame orientation edge.
**D. Launch-necessary:** legal (Privacy/Terms, OFF/ODbL attribution screen) · FTC/health-claim language review (before chat ships publicly) · SafeSide store branding · tests/CI/stability · rotate keys + move to a prod Supabase project.
**E. Phase D (LAST, founder's call):** Sign in with Apple + account linking + in-app account deletion · RevenueCat paywall (price-before-signup, trial, one-tap cancel, Restore) · App Store submission.

---

## How to build / run (macOS + Xcode 26.6)
Project is generated by **XcodeGen** from `ios/project.yml` (the `.xcodeproj` is gitignored — regenerate it).

```sh
brew install xcodegen deno supabase/tap/supabase   # if missing
cd ios
cp Config-example.xcconfig Config.xcconfig         # then fill in values (below)
xcodegen generate                                  # ALWAYS regenerate before building
open FoodScanner.xcodeproj                          # or use xcodebuild
```
Signing: Personal Team `JPS4HAKT9Z` is baked into `project.yml` (swap for a paid team before TestFlight). Device: enable Developer Mode + Trust the developer cert once.

CLI build/install to the physical device (bundle `io.omnisai.foodscanner`):
```sh
cd ios && xcodegen generate            # gotcha: the .xcodeproj sometimes vanishes between steps — regenerate + confirm it exists right before xcodebuild
xcodebuild -project FoodScanner.xcodeproj -scheme FoodScanner \
  -destination 'platform=iOS,id=<DEVICE_UDID>' -allowProvisioningUpdates build
xcrun devicectl device install app --device <DEVICE_UDID> \
  ~/Library/Developer/Xcode/DerivedData/FoodScanner-*/Build/Products/Debug-iphoneos/FoodScanner.app
```
**Screenshot-verify workflow (DEBUG):** launch on the simulator with `SIMCTL_CHILD_SHOW_SCREEN=me|plan|onboarding|result|scan` (or `SHOW_SAMPLE_RESULT=1`) to boot straight into one screen, then `xcrun simctl io booted screenshot`. Verify layouts on the sim BEFORE pushing to device. The scanner overlay needs a real camera (sim can't show it).

## Config / secrets (NOT in the repo)
Recreate `ios/Config.xcconfig` from `Config-example.xcconfig`:
- `SUPABASE_URL = https:/$()/usmdthxnxzdywtjgbokl.supabase.co`  (note the `$()` escape for the `//`)
- `SUPABASE_ANON_KEY = <publishable key>` — the publishable/anon key (safe in the binary); ask the founder or read from the Supabase dashboard → API settings.

**Supabase project ref:** `usmdthxnxzdywtjgbokl`. Deploy backend:
```sh
export SUPABASE_ACCESS_TOKEN=<sbp_… personal access token>   # from Supabase dashboard → Access Tokens
supabase functions deploy product ingredients product-ocr chat search swaps --project-ref usmdthxnxzdywtjgbokl
# migrations/seed applied via the Management API SQL endpoint (see MEMORY.md) or `supabase db push`
#   Chunk 3 adds migrations 20260710000000_products_categories + seed 20260710010000_swaps_demo_seed.
```
**Edge-function secrets already set on the project (values are secret — rotate the pasted ones):** `LLM_BASE_URL` + `LLM_API_KEY` + `LLM_MODEL` (currently Cerebras `gpt-oss-120b`; OpenAI-compatible, swappable to Groq/OpenRouter/Gemini), `USDA_API_KEY`, plus the platform `SUPABASE_*`. **⚠️ Rotate the Cerebras / Groq / USDA keys and the Supabase access token** — they were pasted in chat during development.

## Key gotchas
- **`ios/FoodScanner.xcodeproj` disappears** between build steps in this environment — always `xcodegen generate` and confirm it exists immediately before `xcodebuild`, or you'll reinstall a stale binary.
- **AVCaptureSession:** never call `startRunning()` between `beginConfiguration`/`commitConfiguration` (crashes signal 6). Commit explicitly before start.
- **Scoring rounding** is half-up (Excel `ROUND`), not banker's. Additive penalties count first/additional per tier.
- **LLM never invents** scores/nutrition/ingredient facts — it only rewrites vetted KB/DB data. Keep it that way.
- Full deno tests: `cd supabase/functions && deno task test`. iOS unit tests: `-only-testing:FoodScannerTests` (full `test` incl. XCUITest can time out on sim boot).
